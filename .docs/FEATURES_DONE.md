_Mỗi khi hoàn thành task, phải dùng lệnh /save để cập nhật vắn tắt vào đây_

# Sổ tay tiến độ dự án (FEATURES_DONE)

- _2026-08-18_: Lịch sử thi thử chuyển sang phân trang server 6 dòng/trang qua `/attempts/me?page=&limit=&type=MOCK_TEST`; dashboard lấy summary từ backend thay vì tải toàn bộ lịch sử. Sau submit SKILL_FULL_SET, Reading/Listening/Grammar/Writing/Speaking dùng `/attempts/{id}/review` để lấy đáp án/bài mẫu có kiểm tra owner. Xóa các service student `/questions` cũ không còn dùng, lazy-load route cho 5 kỹ năng/Mock Exam/Admin và thêm PostCSS Tailwind v4. Production build/typecheck đạt; CSS directive warnings đã hết.

## Phiên bản: 1.0.0 (Khởi tạo)

### 🚀 Giai đoạn 0: Khởi tạo hệ thống (Setup Base)

- [x] Phân tích toàn bộ Source Code hiện tại.
- [x] Thiết lập cấu trúc thư mục chuẩn Domain-Driven Design (DDD).
- [x] Tạo bộ khung hướng dẫn AI (.agent/ & .docs/).
- [x] Cấu hình Axios Interceptors & React Query Base.
- [x] Thiết lập Ant Design ConfigProvider & Theme.

### 📦 Các thành phần tái sử dụng (Reusable Assets)

#### Components

- `TableWithPagination`: Bảng dữ liệu tích hợp phân trang chuẩn Antd.
- `PageContainer`: Layout bọc trang tích hợp Breadcrumb.
- `ConfirmDialog`: Modal xác nhận hành động nguy hiểm.
- `RequiredLabel`: Nhãn đánh dấu trường bắt buộc trong Form.

#### Hooks

- `useFilter`: Quản lý state bộ lọc và phân trang.
- `useI18n`: Hook hỗ trợ đa ngôn ngữ nhanh.
- `usePageTitle`: Tự động cập nhật tiêu đề trang.

---

- _2026-07-09_: **Nối API nộp bài + chấm điểm + AI cho trang học viên** (`POST /exams/:id/submit`): thêm lớp dùng chung [shared/services/student-exam] và mapper đáp án cho 5 kỹ năng. MOCK_TEST/SKILL_FULL_SET/PART_PRACTICE đều dùng contract submit hiện hành; Speaking chỉ gửi URL bản ghi thật. Response thành công hiển thị đầy đủ điểm AI; lỗi AI làm request thất bại và không persist kết quả dở dang.
- _2026-07-09_: **Thi thử Mock Exam full 5 kỹ năng — nối API (hiển thị đề, chưa chấm điểm)** ([apps/full-mock-exam]): service [mockExamApi.ts] (`GET /exam-sets?type=MOCK_TEST&isActive=true` + `GET /exam-sets/{id}`)/[mockExamQuery.ts]. [mockExamMapper.ts] `buildFullMockExam`: cắt exam theo `section.skillId` (1..5) rồi **tái dùng builder của từng app luyện tập** (`buildGrammarExam`/`buildListeningExam`/`buildWritingPrompts`/`buildSpeakingExam`; Reading dựng riêng **đủ 5 phần API — P2 & P3 cùng dạng ORDERING**), lấy `durationMinutes` thật từng section. Landing thay 4 đề hardcode bằng đề thật (Spin/Empty). Trang thi chính: fetch đề theo `testId`, danh sách kỹ năng **động** (chỉ hiện kỹ năng có câu hỏi, thứ tự Nói→Nghe→Ngôn ngữ→Đọc→Viết), đếm câu/phần thật, timer theo duration thật. 5 section viết lại nhận data qua props: **GrammarVocab** (grammar từng câu + vocab mỗi task 1 số, SENTENCE inline), **Listening** (audio thật `mediaUrl` + player play/pause, P1 câu lẻ, P2 speakers, P3 statements, P4 monologue), **Reading** (P1 đoạn văn inline gap, P2/P3 kéo-thả, P4 speaker-match, P5 heading-match), **Writing** (prompts động theo part, đếm từ theo giới hạn API, P4 2 email), **Speaking** (ảnh thật, prep/record theo cấu hình, P4 cả bộ 1 lượt nói). Xóa `speakingData.ts` mock. Chưa chấm điểm — nộp bài chỉ ghi nhận. Lint + typecheck sạch (trừ 2 file pre-existing AudioWaveform/SpeakingController ngoài phạm vi).
- _2026-07-09_: **Bảng câu hỏi Grammar & Vocab — mỗi task vocab = 1 số (không tách 5 số)**: [grammar-mock-test] & [grammar-part2] đổi nav sang "đơn vị hiển thị" — grammar mỗi câu 1 số (1..25), vocab **mỗi task (1 bản ghi WORD_BANK) = 1 số** (26..30); ô task tô "đã trả lời" khi đủ 5/5 ý, tooltip hiện "đã trả lời x/5 ý"; footer đếm theo đơn vị (VD "Câu 26 trên 30"), nút trước/sau nhảy theo task; badge trong card vocab đổi từ số toàn cục (26..50) → số ý trong task (1..5); tiến độ/chấm điểm vẫn theo tổng số ý. `QuestionNav` cả 2 trang nhận `items` (display/answered/active/tooltip). Lint + typecheck sạch.
- _2026-07-09_: **Form tạo đề Grammar & Vocab — mỗi Task chỉ chọn 1 bản ghi**: [GrammarSelection.tsx] cột cấu trúc đề đổi từ 5 slot/task → **1 slot/task** (1 bản ghi WORD_BANK = bộ 5 câu, hiển thị "1/1 (bộ 5 câu)"); [useCreateExam.ts] `handleAddQuestion` với Vocabulary **luôn thay thế** bản cũ cùng task (kể cả chế độ partial). Phía học viên xác nhận hiển thị chuẩn: cả trang luyện theo phần lẫn theo bộ đề dùng chung `mapVocabularySets` (SENTENCE inline `_______`, 4 task còn lại dạng dòng), số câu đánh liên tục qua task. Lint + typecheck sạch.
- _2026-07-09_: **Chuẩn hóa 5 task Vocabulary (Grammar P2) theo đề APTIS thật**: cập nhật [QUESTION_SAMPLES.md] — `content` = câu lệnh instruction chuẩn APTIS từng task (Complete each definition… / Select a word… most often used… / Finish each sentence… / most similar meaning / opposite meaning); Task SENTENCE quy ước `prompt` chứa chỗ trống **`_______`** (7 gạch) + samples viết lại đúng format ảnh đề thật; ghi chú đề thật có thể dùng 2 task SYNONYM thay ANTONYM. [questionMapper.ts] `buildGrammar` tự điền instruction mặc định theo `task_variant` khi admin bỏ trống content. [GrammarForm.tsx] label/placeholder từng task theo ví dụ thật, dọn lint (`FormInstance`, bỏ `TextArea`/`as any`, `titlePlacement`). FE học viên [grammar-practice/mappers.ts] `mapVocabularySets`: task **SENTENCE render dropdown inline giữa câu** (type `context`, chuẩn hóa `_{2,}` → `_______`, câu thiếu chỗ trống tự thêm cuối câu), 4 task còn lại dạng dòng trái/phải; instruction fallback chuẩn APTIS — áp dụng cả trang luyện theo phần lẫn theo bộ đề (dùng chung mapper). Lint + typecheck sạch.
- _2026-07-08_: **Luyện theo bộ đề — Speaking (học viên)** ([apps/speaking-practice]): nối API thật cho tab "Luyện tập theo bộ đề". Thêm service/query [speakingPracticeApi.ts]/[speakingExamQuery.ts] dùng `GET /exam-sets?skillId=5` và `GET /exam-sets/{id}`. Thêm [speakingExamMapper.ts] flatten sections→parts→questions và dựng dữ liệu theo schema Speaking thật: **P1** các câu độc lập, **P2/P3/P4** mỗi bản ghi là một bộ có `image_urls[]` + `questions[]`. Landing thay `mockTestsData` bằng đề thật (loading `Spin` + empty `Empty`), bấm "Bắt đầu làm đề" → `/speaking/mock-test/$testId`. Trang làm đề ([speaking-mock-test]) dùng API thay mock cứng và giữ trình bày giống luyện theo phần: **P1** từng câu cá nhân 30s, **P2** 1 ảnh + subtab câu con, **P3** 2 ảnh + subtab câu con, **P4** danh sách 3 câu cùng một lượt ghi âm 60s chuẩn bị + 120s nói; Part 4 ghi âm xong đánh dấu toàn bộ câu con của bộ. Nav/báo cáo/timer/đáp án mẫu sau nộp đều động theo dữ liệu đề. Build/typecheck sạch; lint riêng các file mới/sửa sạch.
- _2026-07-08_: **Luyện theo bộ đề — Writing (học viên)** ([apps/writing-practice]): nối API thật cho tab "Luyện tập theo bộ đề", dựng câu hỏi động theo schema và nộp ESSAY qua API. Tiến độ/điểm tốt nhất hiện lấy từ database qua progress/attempt query; localStorage không còn là nguồn tiến độ.
- _2026-07-08_: **Luyện theo bộ đề — Reading (học viên)** ([apps/reading-practice]): nối API thật để nạp đề lên. Service [readingPracticeApi.ts] (`GET /exam-sets?skillId=3` danh sách đề + `GET /exam-sets/{id}` chi tiết full tree)/[readingExamQuery.ts]. [readingExamMapper.ts] `flattenExam`: duyệt sections→parts→questions, **giữ đủ 5 phần theo API (partNumber 1..5)**, gom câu hỏi theo part. Landing tab "Luyện tập theo bộ đề" ([reading-landing]) thay `mockTestsData` bằng đề thật (loading `Spin` + empty `Empty`), bấm "Bắt đầu làm đề" → `/reading/mock-test/$testId`. Trang làm đề ([reading-mock-test]) `useMockTest` dựng **5 phần động**: **P1** gap-fill render **đoạn văn thật với ô chọn inline** (`___(n)` chèn Select đúng vị trí, hoặc `___` thay tuần tự, hoặc liệt kê câu — giống trang luyện theo phần), **P2 & P3** cùng dạng ORDERING kéo-thả (2 bộ slot/pool độc lập, drag chung `dragPart`), **P4** speaker-match, **P5** heading-match. Nav/nút điều hướng chỉ chạy giữa các phần **có dữ liệu** (`availableParts`); timer + chấm điểm 5 phần + báo cáo. Lint + typecheck sạch.
- _2026-07-05_: Đồng bộ UI + dữ liệu thật cho admin:
  - **Luyện Ngữ pháp & Từ vựng theo phần (học viên)** ([apps/grammar-practice]): nối API thật `GET /questions?skillId=1&partNumber=`. Service [grammarApi.ts]/[grammarQuery.ts]/[mappers.ts]: **P1** MC (mỗi bản ghi 1 câu) → chấm điểm thật, tổng câu **động** (không cứng 25); **P2** WORD_BANK **4-5 task** — mỗi bản ghi = 1 task (task_variant DEFINITION/COLLOCATION/SENTENCE/SYNONYM/ANTONYM, `slots[5]` + `options_pool[10]`), **đánh số câu chạy liên tục 1..N** qua các task, chấm điểm thật. `QuestionNav` (part1 & part2) nhận `questionNumbers`/`sectionLabel` **động** thay dải cứng 1-25/26-50; footer/tiến độ/modal điểm dùng tổng động; loading `Spin` + empty `Empty`; xóa 2 `mockExamData.ts`. (Trang vốn đã là luyện theo phần, không nhảy sang phần khác.)
  - **Luyện Nghe theo phần (học viên)** ([apps/listening-practice]): nối API thật `GET /questions?skillId=2&partNumber=`. Service [listeningApi.ts]/[listeningQuery.ts]/[mappers.ts]: P1 MC (mỗi bản ghi 1 câu + audio riêng), P2 SPEAKER_MATCH (options_pool + 4 người), P3 SPEAKER_AGREEMENT (statements Man/Woman/Both), P4 Monologue (mỗi bài nghe 1 bản ghi, questions[] MC). Cuộn **trong cùng part** qua nút **Câu/Bài tiếp theo** + tag "Bài X/Y" (P1 cuộn câu; P2/P3/P4 cuộn theo bản ghi/bài nghe), **không nhảy part**; nộp bài chỉ ghi nhận. [AudioPlayer.tsx] thêm prop `src` phát audio thật (`mediaUrl`); loading `Spin` + empty `Empty`; bỏ transcript (API không có); xóa 4 `data.ts` mock.
  - **Speaking — đổi cách lưu**: P1 giữ tách lẻ (mỗi câu 1 bản ghi); P2/P3/P4 gói toàn bộ câu hỏi vào `extraConfig.questions[]` (`{question, sample_answer?}`) — 1 bản ghi/part. `buildSpeaking` ([questionMapper.ts]) cập nhật; `RecordConfig.questions[]` + bỏ `question_group_id` (types đã có sẵn). Listening P3 (`statements[]`)/P4 (`questions[]`) đã đổi từ trước.
  - **Luyện Nói theo phần (học viên)** ([apps/speaking-practice]): nối API thật `GET /questions?skillId=5&partNumber=`. Service [speakingApi.ts]/[speakingQuery.ts]/[mappers.ts]: P1 → danh sách câu độc lập; P2/P3/P4 → danh sách "bộ" (ảnh + `questions[]`). Cuộn **trong cùng part** (P1: câu↔câu; P2/P3: sub-question rồi sang bộ khác; P4: bộ↔bộ) qua nút **Câu/Bộ tiếp theo** + tag "Bộ X/Y", **không nhảy sang part khác**; nộp bài chỉ ghi nhận. Loading `Spin` + empty `Empty`; đáp án mẫu (nếu có `sample_answer`) hiện trong "Xem đáp án mẫu", ẩn khi rỗng.
  - **Fix upload ảnh Speaking (2 lỗi 400)**:
    1) [questionApi.ts] `upload` trước ép `Content-Type: 'multipart/form-data'` thủ công → thiếu `boundary` → 400. Sửa: `'Content-Type': null` cho axios tự sinh header kèm boundary (áp dụng cả audio Listening).
    2) [SpeakingForm.tsx] `Form.Item name="p*ImageUrl"` **bọc trực tiếp `Upload.Dragger`** → antd ghi đè giá trị field bằng object `{file,fileList}` (không phải URL) → `image_urls` rỗng → 400 "phải có ít nhất 1 phần tử". Sửa: tách Upload khỏi Form.Item có name; dùng **field ẩn `<Input hidden>`** giữ URL (cập nhật qua `setFieldValue`). Dọn `any`/biến thừa trong form.
  - **Writing — đổi cách lưu (mỗi part = 1 bản ghi)**: theo QUESTION_SAMPLES.md cập nhật. `EssayConfig` thêm `prompts[]` (P1/P3) + `tasks[]` (P4) + `context`; [questionMapper.ts] `buildWriting` gộp P1 (5 câu→`prompts`), P3 (3 Member→`prompts` kèm `speaker_name`), P4 (Notice `context` + `tasks[2]` Informal/Formal) thành 1 POST/part; [QuestionDetailModal.tsx] render prompts/tasks.
  - **Luyện Viết theo phần (học viên)** ([apps/writing-practice]): nối API thật `GET /questions?skillId=4&partNumber=` (trước mock cứng). Service [writingApi.ts]/[writingQuery.ts]/[mappers.ts] map `extraConfig` → shape từng trang (P1 prompts→5 câu, P2 content→đề, P3 prompts→3 chat, P4 context+tasks→Notice+2 email). Giới hạn từ lấy động từ API (validation + badge); loading `Spin` + empty `Empty`. Writing giữ đủ 4 phần.
    - **Đáp án mẫu (`sample_answer`)**: thêm trường ở form admin ([WritingForm.tsx]: ô đáp án mẫu cho từng câu P1/P3, đề P2, từng task P4) → lưu vào `extraConfig` (prompt/task `sample_answer`, P2 top-level); [questionMapper.ts]/[types.ts]/[QuestionDetailModal.tsx] cập nhật. Trang học viên có nút **"Xem đáp án mẫu"** (chỉ hiện khi có dữ liệu) mở `SampleAnswerModal`.
    - **Luyện nhiều câu/phần (như Reading)**: mỗi hook giữ danh sách câu của phần + chỉ số; nút **Câu trước/Câu tiếp theo** + tag "Câu X/Y"; nộp bài chỉ chấm câu hiện tại (không còn tự nhảy sang part khác), nút "Danh sách" về `/writing`.
  - **Luyện Đọc theo phần (học viên)** ([apps/reading-practice]): nối API thật cho 4 phần luyện tập (trước là mock cứng trong từng `services/data.ts`). Lớp service dùng chung [readingApi.ts]/[readingQuery.ts]/[mappers.ts]: fetch `GET /questions?skillId=3&partNumber=` (role đã tắt ở BE dev), mapping FE→API **1→1, 2→2, 3→4, 4→5** (bỏ API part 3 vì trùng dạng ORDERING với part 2 → còn 4 phần riêng biệt). Mapper chuyển `extraConfig` về đúng shape từng trang: P1 gap-fill (render đoạn văn động tách theo `___(n)`), P2 ORDERING (kéo-thả, tách câu cố định + xáo pool), P3 SPEAKER_MATCH người/ý kiến (radio động theo số người), P4 HEADING_MATCH (gán tiêu đề). Mỗi trang thêm loading `Spin` + empty `Empty`, chấm điểm client-side như cũ; xóa 4 file `data.ts` mock. **Luyện nhiều câu/phần**: mỗi hook giữ danh sách câu hỏi của phần + chỉ số hiện tại, có nút **Câu tiếp theo/Câu trước** + tag "Câu X/Y" (chuyển câu reset đáp án/timer). Part 1: placeholder ô chọn "Chọn đáp án", giữ nguyên xuống dòng gốc của đoạn văn (`white-space: pre-line`); render Select **ngay tại chỗ trống** — hỗ trợ cả `___(n)` (theo số) lẫn `___` gạch chân thường (thay lần lượt theo thứ tự câu), fallback liệt kê khi đoạn không có chỗ trống. Part 4: sửa ô chọn tiêu đề bị co nhỏ do class Tailwind không chạy → dùng styled `SlotInner` + `style width:100%`, giãn cách hợp lý. Bỏ Alert kết quả kiểu "mọc ra đẩy nội dung" ở cả 4 part, thay bằng **tag kết quả cố định trên header** (không xô lệch layout).
  - **Tài liệu học tập (trang học viên)** ([apps/materials]): nối API thật `GET /study-materials` (trước chỉ có ở admin, học viên còn `mockMaterials`). Service riêng read-only (materialApi/materialQuery), hook [useMaterials.ts] fetch (limit 100) + map về card (skillId→nhãn tiếng Việt khớp tab, VIDEO hiện thời lượng mm:ss), lọc search/định dạng/kỹ năng client-side. **Đa định dạng**: util [fileFormat.ts] `getFileMeta` suy nhóm file thật từ đuôi `fileUrl` (pdf/word/excel/ppt/video/image/audio/archive/text/…), fallback `fileType` thô của API → icon + màu + nhãn riêng; bộ lọc định dạng dựng **động** theo các nhóm thực có trong dữ liệu. Nút tải mở `fileUrl` (`window.open`), thêm loading `Spin`. Xóa `mockData.ts`.
  - **Ngân hàng câu hỏi — chỉnh sửa thật** ([QuestionEditModal.tsx]): trước đây nút Sửa chỉ mở lại form thêm mới rỗng. Nay modal sửa riêng đổ dữ liệu từ `raw.extraConfig` theo từng dạng (MC chọn đáp án đúng, gap-fill 5 chỗ, ORDERING nhập theo thứ tự đúng, WORD_BANK pool+slots, SPEAKER_MATCH Listening/Reading, HEADING_MATCH, Man/Woman/Both, ESSAY giới hạn từ + context, RECORD thời gian) + content/mediaUrl; lưu qua `PATCH /questions/{id}` (`useUpdateQuestionMutation` đã có sẵn, nay mới dùng). Form `key={id}` + `preserve=false` tránh dính dữ liệu giữa các câu. Modal thêm mới giữ nguyên cho flow "gộp".
  - **Quản lý người dùng**: chi tiết người dùng chuyển từ `Drawer` trượt phải sang **Modal popup** ([UserDetailModal.tsx]) đồng bộ phong cách với `QuestionDetailModal` (icon tiêu đề, `Descriptions`, footer "Đóng", `centered`). Đổi tên state `drawer*` → `detail*` trong [useUsers.ts]; xóa styled `DrawerHeader` thừa.
  - **Tạo bộ đề — bước "Xem trước & Xuất bản"**: bỏ mockup A/B/C/D giả, render **dữ liệu thật** từ câu hỏi đã chọn. `IBankQuestion` mang thêm `raw: IQuestion` ([examBank.ts]); component mới [ExamPreview.tsx] render nội dung + đáp án đúng theo từng dạng (MC, gap-fill, WORD_BANK, ORDERING, SPEAKER_MATCH, HEADING_MATCH, ESSAY, RECORD kèm audio/ảnh). Audio chỉ hiện cho Listening (skillId 2), ảnh chỉ cho Speaking (skillId 5). Tiêu đề/kỹ năng/phần/thời gian lấy từ cấu hình bước 1 (tự suy title cho loại "Luyện theo phần" khi không có tên).
  - **Chi tiết/chỉnh sửa đề** ([ExamDetail.tsx]/[ExamPartEditor.tsx]/[ExamSectionCard.tsx]): làm thoáng layout (Space vertical size large, nút "Lưu thông tin" canh phải, "Lưu phần" đưa lên header phải của part, thêm khoảng cách giữa các khối/section, nhãn "Danh sách câu hỏi"). List thao tác đề ([useExamColumns.tsx]): thêm icon **Chỉnh sửa** (`EditOutlined`) cạnh Xem/Xoá, gom `renderActions` + `Tooltip` + giãn nút; bỏ hết `any` (dùng `ExamRow`).
  - **Xem vs Chỉnh sửa (chế độ chỉ đọc)**: nút Xem điều hướng `?mode=view` → [useExamDetail.ts] đọc `useSearch` trả `readOnly`. Ở chế độ xem: khoá input tiêu đề/mô tả/hướng dẫn (`readOnly`), ẩn toàn bộ nút thao tác (Lưu thông tin, Lưu thời gian, Lưu phần, upload audio, di chuyển/xoá câu hỏi, đổi trạng thái), thời gian hiện dạng text; tiêu đề trang đổi "Xem/Chỉnh sửa đề thi". Nút Chỉnh sửa mở chế độ sửa như cũ. Layout `ExamPartEditor` chuyển sang styled-components ([ExamPartEditor.styled.ts]) fix nút "Lưu phần" bị dính sát.

- _2026-07-04_: Phân trang đồng bộ toàn bộ trang list admin + fix đổi số dòng/trang:
  - Hook dùng chung [usePagination.ts] (quản lý `page` + `pageSize`, `onChange(page,size)` khớp `AppPagination`; đổi pageSize tự về trang 1). **Fix lỗi "10/20 mỗi trang chưa ăn"** — trước đây chỉ truyền `setPage` nên bỏ qua tham số pageSize.
  - Nối phân trang thật (page/limit + `metaData.total`) cho: `admin-questions` (theo skill+part, thêm `questionApi.listPaged` giữ `list` mảng cho ngân hàng dựng đề), `admin-exams` (query theo tab loại đề), `admin-users`, cùng materials/faq/notifications đã làm trước.

- _2026-07-04_: Bổ sung phân trang + endpoint mới (Phase 7 & 8 cập nhật):
  - **axios**: thêm cờ `_rawEnvelope` + export `IApiEnvelope`/`IPageMeta` — list phân trang lấy nguyên envelope `{ data, metaData }` (mặc định vẫn trả `data` như cũ, không phá code hiện có).
  - **Notifications admin**: chuyển sang endpoint mới `GET /notifications` (kèm `receiver`), phân trang thật + lọc `audience` (broadcast/personal) + `notificationType`; cột "Phạm vi" hiện email người nhận. Bỏ badge "chưa đọc" ở trang quản lý (không hợp ngữ nghĩa toàn hệ thống).
  - **Study Materials**, **FAQ admin**: phân trang thật (`page/limit` + `metaData.total` cho `AppPagination`) thay cho đếm `.length`.

- _2026-07-04_: Nối API Users (Phase 1) cho `admin-users` + dọn dashboard admin:
  - Service (userApi/query/types `IAdminUser`/`IUserRow`) + `GET/POST/PATCH /users` + `PATCH /users/{id}/lock`. Danh sách fetch thật, tạo người dùng (email/password/full_name/role), khoá/mở khoá. Model đồng bộ với các trang khác (role/status/profile.aptisGoal), chỉ **`streak` (chuỗi ngày) tạm fake** theo id vì chưa có API.
  - Cột bỏ "Gói"/"Tiến độ" (không có dữ liệu), thêm "Vai trò"; drawer chi tiết thay tab progress/history/files giả bằng `Descriptions` model thật; UserModal bỏ Gói/SĐT/ngày sinh, dùng `role`. Xóa `initialStudents` mock.
  - Dashboard admin (nhóm C): bỏ cột "Gói" (thay "Ngày đăng ký"), dọn timeline khỏi sự kiện tính năng không tồn tại (duyệt tài liệu, nâng gói, giáo viên chấm).

- _2026-07-04_: Nối API Phase 8 — FAQ (admin + học viên):
  - **Admin** (`admin-faq`): service (faqApi/query/types) + `GET/POST/PATCH/DELETE /faqs`. List dùng `includeInactive=true` (admin thấy cả FAQ ẩn), hook [useFaq.ts] tạo/sửa/xóa mềm. Form + cột chuyển từ mock `status` sang field thật `isActive` (boolean) + `sortOrder`; cột/param type theo `IFaq` (bỏ `any`).
  - **Học viên** (`apps/faq`): service riêng (chỉ list FAQ đang bật, không gửi `includeInactive`), hook [useFaqList.ts] fetch thật + lọc category/search client-side + trạng thái mở accordion. Thay `mockFaqs`, thêm loading spinner.
  - **Thông báo học viên** (Sidebar): service [home/services/notification.ts] + hook [home/hook/useNotifications.ts] nối `GET /notifications/me` (chỉ khi đã đăng nhập), `PATCH /{id}/read` (click từng cái, broadcast không đánh dấu riêng), `read-all` (nút "Đánh dấu đã đọc"). Popover chuông bỏ mảng hardcode, group theo ngày (Hôm nay/Hôm qua/Tuần này/Trước đó) + thời gian tương đối; badge số = số chưa đọc (ẩn khi 0), có empty state.

- _2026-07-04_: Chi tiết câu hỏi + Phase 7 (Materials/Notifications/Settings):
  - **Chi tiết câu hỏi** ([QuestionDetailModal.tsx]): viết lại render theo đúng loại câu hỏi từ `raw.extraConfig` — MC, gap-fill, WORD_BANK, ORDERING, SPEAKER_MATCH (Listening & Reading), Man/Woman/Both, HEADING_MATCH, ESSAY, RECORD (kèm audio player / ảnh). Không còn A/B/C/D giả.
  - **Study Materials** ([admin-materials]): service (materialApi/query/types) + CRUD `/study-materials`; upload PDF/AUDIO/VIDEO/DOCX/PPTX/XLSX/ZIP hoặc dùng LINK; có lọc, tìm kiếm, phân trang, sửa, xóa và mở tài liệu.
  - **Notifications** ([admin-notifications]): service thật; list `GET /notifications/me` + badge chưa đọc, gửi `POST /notifications` (broadcast/gửi riêng receiverId, type SYSTEM/EXAM_REMINDER/GRADE_RESULT), `PATCH /{id}/read` + `read-all`. Bỏ edit/delete (không có API), xóa `notificationService.ts` mock.
  - **Settings** ([admin-settings]): tab "Thời gian thi thử" nối `GET /settings` + `PATCH /settings/{key}` cho 5 key `MOCK_TEST_DURATION_*`. Các tab general/packages/logs vẫn là mock (ngoài phạm vi API).

- _2026-07-03_: Nối API Phase 4 — Exam Sets (dựng đề) cho `admin-exams`:
  - Service: `services/types.ts` (map ui type partial/set/full ↔ PART_PRACTICE/SKILL_FULL_SET/MOCK_TEST, skill label ↔ id), `examApi.ts` (exam-sets CRUD + toggle-active + assign/reorder/remove question + patch section/part), `examQuery.ts`, `examBank.ts` (map câu hỏi API → shape Selection, gồm Vocabulary task_variant → Task 1-5).
  - List ([useExams.ts]): fetch thật `/exam-sets`, tách 3 tab theo `type`, xóa + toggle-active thật (bấm tag trạng thái để bật/tắt hiển thị).
  - Create ([useCreateExam.ts]): publish flow thật = `POST /exam-sets` (BE tự sinh sections/parts) → gom câu đã chọn theo `(skillId-partNumber)` map tới `part.id` → `POST /exam-parts/{id}/questions` → `toggle-active`. Ngân hàng câu hỏi ở bước chọn lấy thật từ `/questions` (thay `mockBankQuestions`), truyền xuống 5 Selection component qua prop `bankQuestions`.
  - Chi tiết/sửa đề (route `/admin/exams/$examId`, [ExamDetail.tsx] + [useExamDetail.ts] + `ExamSectionCard`/`ExamPartEditor`): `GET /exam-sets/{id}` full tree; sửa title/description (`PATCH /exam-sets/{id}`); toggle-active; đổi `durationMinutes` từng section (`PATCH /exam-sections/{id}`); sửa `instruction` + upload/gán `audioUrl` chung part cho Listening P3/P4 (`PATCH /exam-parts/{id}`); gỡ câu hỏi (`DELETE`) + đổi thứ tự lên/xuống (`PATCH .../reorder`). Nút "xem" ở list mở trang này.
  - _Còn tồn_: gán thêm câu hỏi vào đề đã tạo từ màn chi tiết (mới có gỡ/sắp xếp); `mockData.ts` còn export thừa chưa dùng.

- _2026-07-03_: Nối API Auth + Profile + Question Bank (React Query, không dùng fetch):
  - **Auth**: `apps/auth/services` (authApi/authQuery/types) cho login/register/refresh/logout/change-password/Google; axios interceptor tự refresh token, guest-mode (401 âm thầm trừ login/register), Redux `authSlice` + `useLogout` xóa cache khi đổi phiên; trang `/oauth` callback Google.
  - **Profile**: `GET/PATCH /profile/me` khớp `user_profiles` (full_name, target_date, aptis_goal, school_name).
  - **Question Bank** (`apps/admin/pages/admin-questions/services`): types 19 dạng extraConfig + questionApi (CRUD `/questions` + `/files/upload`) + questionQuery + **questionMapper** so khớp form 5 kỹ năng → payload theo QUESTION_SAMPLES (mở rộng form "gộp" thành mảng POST: Grammar 25 câu, Listening P3/P4 nhiều bản, Writing P4 2 task cùng question_group_id...). Bảng câu hỏi fetch thật theo skill+part, có xóa.
  - _Còn tồn_: Reading P5 heading-match cần form thu thập "câu 0" mẫu (đang sinh example tối thiểu); upload ảnh/audio Speaking/Listening cần wire `useUploadFileMutation` vào antd Upload để lấy URL thật.

- _2026-05-17_: Xây dựng 4 màn hình luyện tập Đọc hiểu tương tác đỉnh cao (Reading Practice Sub-Pages):
  - Phân rã cấu trúc và tổ chức 4 trang luyện tập thành các thư mục riêng biệt (`part1/`, `part2/`, `part3/`, `part4/`) dưới thư mục `src/apps/reading-practice/pages/parts/`:
    * Mỗi thư mục tự quản lý file giao diện chính (`Index.tsx`) và file định nghĩa kiểu dáng CSS-in-JS (`styled.ts`) giúp tối ưu hóa khả năng bảo trì và nâng cấp.
    * **Part 1 (Sentence Comprehension)**: Bố cục 2 cột trực quan. Cột trái mô phỏng Email thực tế, cột phải gồm 5 câu hỏi tách biệt dạng thẻ Card đi kèm ô chọn `<Select>` inline tối ưu 16px.
    * **Part 2 & 3 (Text Cohesion)**: Hệ thống kéo thả sắp xếp cốt truyện bằng **HTML5 Drag and Drop API** bản địa (0 dependencies) có hỗ trợ click chuyển vị trí tự động cho thiết bị di động cực tiện lợi.
    * **Part 3 (Opinion Matching)**: Bố cục 2 cột tinh tế với 4 khối ý kiến của 4 người bên trái và 7 câu phát biểu nối ý bên phải sử dụng Radio.Group (A, B, C, D) 1-click thay thế dropdown cũ.
    * **Part 4 (Long Text Comprehension)**: Phân tách độc lập cuộn trang 2 cột (dual-scroll). Cột trái cuộn đoạn văn dài được đánh số, cột phải ghim cố định Heading Bank xanh ngọc và danh sách gán tiêu đề 100% chiều rộng.
  - Tách biệt 100% mã nguồn styled-components từ các file TSX của 4 phần sang các file `.styled.ts` chuyên biệt trong cùng thư mục (`Part1Page.styled.ts` đến `Part4Page.styled.ts`) giữ cho cấu trúc code gọn gàng, trong sáng và dễ bảo trì.
  - Khắc phục triệt để lỗi TypeScript liên quan đến thuộc tính `size="small"` không chuẩn trên thẻ `<Text>` của Ant Design tại Part 1.
  - Cấu hình và đồng bộ hóa 15-phút đếm ngược thời gian thực trên mỗi màn để tăng cường tính chân thực của bài thi.
  - Định nghĩa và xuất 4 sub-routes mới (`/reading/part/1-4`) qua TanStack Router tại [routes.tsx](file:///d:/react-exam-online/src/apps/reading-practice/routes.tsx) và đăng ký chính thức tại [App.tsx](file:///d:/react-exam-online/src/App.tsx).
  - Tích hợp tính năng tự động chuyển đổi từ Bảng điều khiển Đọc hiểu thẳng đến các màn luyện tập tương ứng.
- _2026-05-17_: Xây dựng học phần Luyện Viết (Writing Practice):
  - Tạo mới module `writing-practice` độc lập, bao gồm các file components (`PartCard`), custom styles và trang Index quản lý tiến trình.
  - Phân tách cấu trúc thành 4 phần thi chuẩn APTIS Writing với nội dung chi tiết:
    * Part 1: Word-level Writing (Viết cấp độ từ).
    * Part 2: Short Text Writing (Viết đoạn văn ngắn).
    * Part 3: Three Written Parts of a Text (Viết tương tác mạng xã hội).
    * Part 4: Formal and Informal Writing (Viết email trang trọng & thân mật).
  - Quản lý trạng thái tiến độ độc lập bằng React State cục bộ theo yêu cầu (không dùng Redux) đảm bảo sự mượt mà và tương tác tức thì trên giao diện.
  - Tích hợp điều hướng `/writing` qua hệ thống TanStack Router, đăng ký route tại [App.tsx](file:///d:/react-exam-online/src/App.tsx) và tích hợp hoàn chỉnh vào Sidebar và Bảng điều khiển.
- _2026-05-17_: Tái cấu trúc quản lý trạng thái bằng Redux Toolkit chuyên nghiệp:
  - Thiết kế logic tự động tính toán lại Chỉ số thống kê (`overallProgress`, `completedModules`) bất cứ khi nào tiến trình của một phần thay đổi.
  - Đồng bộ hóa toàn bộ 5 trang luyện tập giúp tiến độ bài học phản hồi tức thì và cập nhật trực tiếp lên Bảng điều khiển/Sidebar theo thời gian thực.
- _2026-05-17_: Tái cấu trúc chuẩn hóa hệ thống định tuyến (Loại bỏ window.navigateTo):
  - Xóa bỏ hoàn toàn hàm `window.navigateTo` toàn cục không chuẩn khỏi [App.tsx](file:///d:/react-exam-online/src/App.tsx).
  - Cập nhật [Sidebar.tsx](file:///d:/react-exam-online/src/apps/home/components/Sidebar.tsx) và [ModuleGrid.tsx](file:///d:/react-exam-online/src/apps/home/components/ModuleGrid.tsx) sử dụng hook type-safe `useNavigate()` của TanStack Router.
  - Thay thế toàn bộ các sự kiện chuyển trang thủ công ở Header Logo bằng thẻ `<Link to="/">` tối ưu chuẩn SEO tại tất cả 5 trang chính.
- _2026-05-17_: Xây dựng học phần Ngữ pháp & Từ vựng (Grammar & Vocabulary):
  - Tạo mới module `grammar-practice` độc lập, đầy đủ components (`PartCard`), custom styles và routes trỏ đến `/grammar`.
  - Thiết kế chính xác theo cấu trúc ảnh mẫu: gồm Part 1: Grammar (25 câu trắc nghiệm) và Part 2: Vocabulary (25 câu trắc nghiệm) với mô tả và màu sắc tương ứng.
  - Đăng ký route `/grammar` tại [App.tsx](file:///d:/react-exam-online/src/App.tsx) và tích hợp điều hướng hoàn chỉnh vào `Sidebar` và `ModuleGrid`.
- _2026-05-17_: Tách biệt các học phần luyện tập (Reading, Listening, Speaking):
  - Tách học phần gộp trước đây thành 3 module độc lập hoàn toàn: `reading-practice`, `listening-practice`, và `speaking-practice`.
  - Mỗi module tự quản lý types, components (`PartCard`), custom styles và routes riêng biệt để đạt độ cô lập 100%.
  - Đăng ký 3 route độc lập (`/reading`, `/listening`, `/speaking`) tại [App.tsx](file:///d:/react-exam-online/src/App.tsx) thay vì route động trước đây.
- _2026-05-17_: Chuẩn hóa hệ thống routing dự án (Tái cấu trúc TanStack Router):
  - Trích xuất `rootRoute` dùng chung tại [root.ts](file:///d:/react-exam-online/src/shared/router/root.ts) để tránh circular dependency.
  - Chuyển giao khai báo các route con về đúng từng thư mục module: [home/routes.tsx](file:///d:/react-exam-online/src/apps/home/routes.tsx) và [reading-practice/routes.tsx](file:///d:/react-exam-online/src/apps/reading-practice/routes.tsx).
  - Tích hợp hook `useRouterState` vào `Sidebar` để theo dõi và cập nhật trạng thái `active` động của menu và tự động mở submenu tương ứng.
  - Sửa lỗi định tuyến cho các học phần ở `ModuleGrid` chuyển sang định vị theo `module.id` thay vì so khớp tiêu đề thô, đảm bảo an toàn tuyệt đối.
  - Đổi tên học phần "Nói" thành "Nói 1" tại `useHomeData` mà không gây ảnh hưởng định tuyến.
- _2026-05-13_: Hoàn thành thiết lập bộ khung quản lý dự án cho AI Agent.
- _2026-07-09_: **Luyen theo bo de - Listening (hoc vien)** ([apps/listening-practice]): noi API that cho tab "Luyen tap theo bo de". Them service/query [listeningPracticeApi.ts]/[listeningExamQuery.ts] dung `GET /exam-sets?skillId=2` va `GET /exam-sets/{id}`; them [listeningExamMapper.ts] flatten sections->parts->questions roi tai dung mapper Listening theo phan: **P1** MC audio rieng, **P2** speaker match, **P3** Man/Woman/Both, **P4** 2 bai monologue co `questions[]`. Landing thay mock bang de that (loading `Spin` + empty `Empty`), mo `/listening/mock-test/$testId`. Trang lam de dung du lieu API, bang cau hoi dong theo moc **P1: 1-13**, **P2: 14**, **P3: 15**, **P4: 16-17**; footer di tuan tu "Cau tiep theo" trong cung part va doi sang "Phan tiep theo" o cau cuoi part. P4 giu dung trinh bay theo phan: moi bai 16/17 co audio rieng va render du cau con ben trong. Cham diem/bao cao/timer/luu best score theo du lieu that; build/typecheck sach; lint rieng file moi/sua sach.
- _2026-07-09_: **Luyen theo bo de - Grammar & Vocabulary (hoc vien)** ([apps/grammar-practice]): noi API that cho tab "Luyen tap theo bo de". Them service/query [grammarPracticeApi.ts]/[grammarExamQuery.ts] dung `GET /exam-sets?skillId=1` va `GET /exam-sets/{id}`; them [grammarExamMapper.ts] flatten sections->parts->questions, map **P1** Grammar MC va **P2** Vocabulary WORD_BANK, danh lai so cau de Part 2 noi tiep Part 1. Landing thay mock m1/m2/m3 bang de that (loading `Spin` + empty `Empty`), mo `/grammar/mock-test/$testId`. Trang lam de dung API, giu trinh bay theo phan: **P1** tung cau trac nghiem, **P2** hien tung task Vocabulary (du 5 task neu de co 5 ban ghi, moi task 5 cau voi options_pool). Bang cau hoi dong theo Part 1 va tung Task Part 2; footer di tuan tu "Cau tiep theo" trong cung phan/task va doi "Phan tiep theo" khi het Grammar sang Vocabulary. Cham diem/bao cao/timer/luu best score theo du lieu that; build/typecheck sach; lint rieng file moi/sua sach.
- _2026-07-11_: **Admin Settings - Cai dat chung** ([apps/admin/pages/admin-settings]): nang tab "Cai dat chung" tu form mock sang luu that qua `GET /settings` + `PATCH /settings/{key}`. Them cac key `APP_NAME`, `APP_DESCRIPTION`, `APP_LOGO_URL`, `APP_FAVICON_URL`, `SUPPORT_EMAIL`, `SUPPORT_PHONE`, `PRIMARY_COLOR`, `SECONDARY_COLOR`, `MAX_UPLOAD_MB`, `SEND_RESULT_EMAIL`, `SEND_STREAK_EMAIL`, `ENABLE_IN_APP_NOTIFICATIONS`, `SHOW_ANSWER_AFTER_SUBMIT`, `AUTO_SUBMIT_ON_TIMEOUT`, `ALLOW_SAMPLE_ANSWER`. Logo upload qua `POST /files/upload?folder_type=images&prefix=settings/logo`, preview logo/mau tren form. Sua MockDurationSettings khong setState trong render. Lint scoped sach; build/typecheck sach.
- _2026-07-22_: **Bảng câu hỏi cho "luyện theo phần" + lọc đúng type cho "luyện theo bộ đề"**:
  - Thêm component dùng chung [QuestionBoard](file:///d:/react-exam-online/src/shared/components/QuestionBoard/QuestionBoard.tsx) (styled-components, không inline CSS) — panel "Bảng câu hỏi" giống trang luyện theo bộ đề, hỗ trợ trạng thái chưa/đang làm dở/đã trả lời + nhảy nhanh.
  - Gắn bảng câu hỏi vào 16 trang luyện theo phần (Reading/Listening/Writing/Speaking part 1–4). Mỗi hook thêm `goTo(index)` + `boardItems` (Reading/Writing reset đáp án theo bộ nên lưu thêm `doneSets` để tô "đã nộp"). Grammar part 1/2 vốn đã có `QuestionNav` nên giữ nguyên. Đổi `overflow` các `MainContent` dạng grid/editor sang cuộn dọc để bảng luôn thấy được.
  - Sửa lỗi "luyện theo bộ đề lấy cả đề theo phần": 5 API list ([speaking/writing/listening/reading/grammar]PracticeApi.ts) trước chỉ truyền `skillId` nên trả mọi type; nay thêm `type: 'SKILL_FULL_SET'` để chỉ lấy đúng bộ đề đầy đủ.
  - typecheck sạch; lint scoped các file sửa sạch.
- _2026-07-23_: **Sidebar học viên tự thu gọn khi làm bài**:
  - Giữ nguyên toàn bộ màu sắc và phong cách navy hiện có của sidebar.
  - Sidebar tự thu gọn về chế độ chỉ hiển thị icon khi vào các route làm bài: `/{skill}/part/{number}`, `/{skill}/mock-test/{testId}`, `/grammar/test`, `/mock-exam/main/{testId}`. Nút đóng/mở vẫn hiển thị để người dùng chủ động mở lại; khi chuyển sang route bài thi khác, sidebar tự thu gọn lại.
  - Thêm tooltip cho icon khi sidebar thu gọn; click icon kỹ năng trong lúc thi quay về landing của kỹ năng tương ứng. Lint scoped sạch, build/typecheck thành công.
- _2026-07-30_: **Chấm từng câu ngay trong luyện theo phần — Speaking (cả 4 part)**:
  - Hook dùng chung [usePerQuestionGrading.ts](file:///d:/react-exam-online/src/apps/speaking-practice/hooks/usePerQuestionGrading.ts): nộp RIÊNG 1 câu/bộ đang làm qua `POST /exams/:id/submit` (AI Gemini chấm & trả `ai[]` ngay), lưu kết quả theo `key` cục bộ + trạng thái đang-chấm (`gradingKey`).
  - Dumb component [QuestionGradeCard.tsx](file:///d:/react-exam-online/src/apps/speaking-practice/components/QuestionGradeCard.tsx): nút "Chấm câu này" (đổi "Chấm lại câu này" sau khi có điểm) + spinner khi đang chấm + thẻ điểm/band/nhận xét (tái dùng phong cách card AI của mock-test). Nút chỉ bật khi đã thu âm.
  - Ghép vào 4 hook + Index: **P1** chấm theo từng câu (1 URL, khoá `p1-{questionId}`); **P2/P3** chấm cả bộ (mảng URL câu con, khoá `p{n}-set{index}`); **P4** chấm theo bộ (1 bản ghi lặp cho các câu con). Bỏ phụ thuộc vào nộp-cả-bài để xem điểm. `tsc --noEmit` sạch.
- _2026-07-30_: **Fix BE không nhận audio `webm` khi upload/chấm Speaking** (repo `be-exam-online`): trình duyệt ghi âm bằng MediaRecorder xuất `audio/webm;codecs=opus` nhưng `MediaMagicDetector` không nhận diện → upload & load-để-chấm đều bị chặn "Chỉ chấp nhận audio: mp3, wav, m4a, ogg". Thêm nhận diện magic bytes EBML (`1A 45 DF A3`) → `audio/webm/.webm`; thêm `.webm` vào `MediaUploadPolicy.EXTENSIONS` + thông báo chấp nhận. Đã kiểm chứng: Gemini nhận inline `audio/webm` (HTTP 200) và chấm end-to-end (điểm 76, band B1–C1). _Lưu ý: `aiScore:null` trước đó là do Gemini free-tier 429 hết quota, không phải lỗi code — đã nạp credit._
- _2026-07-30_: **Fix Speaking Part 2/3 per-question grading**: give synchronous AI exam submission a dedicated 120-second timeout (instead of the global 10-second timeout), safely handle a missing `ai[]` result, and only show/enable the grading action after every sub-question in the current Part 2 or Part 3 set has a recording.
- _2026-07-30_: **Speaking part-practice AI action cleanup**: replace the footer `Nộp bài` action in Parts 1-4 with an always-visible `Chấm bằng AI` button for the current question/set (disabled until all required recordings are ready), move loading/results into the shared grade card without a duplicate action button, remove the old whole-part submit handlers, and remove the Part 1 `Chọn câu` select while retaining sequential navigation and the question board.
- _2026-07-30_: **Speaking full-set AI completion guard**: the `Chấm bằng AI` action on `/speaking/mock-test/$testId` stays visible but disabled until every question has an uploaded recording. The same full-completion guard is enforced inside the submit request and timeout path, so AI never receives a partial full-set submission; complete payloads still send P1 as individual URLs and P2-P4 as ordered URL arrays for whole-exam grading.
- _2026-07-30_: **Chấm AI trả điểm theo từng tiêu chí (Speaking & Writing)** (cross-repo `be-exam-online` + FE): trước đây kết quả chấm AI chỉ có 1 điểm tổng + 1 nhận xét vì JSON schema Gemini chỉ yêu cầu `score`/`band`/`feedback`. Nay bổ sung end-to-end mảng `criteria` — rút gọn còn **3 tiêu chí cốt lõi/kỹ năng** (vừa gọn form vừa tiết kiệm token).
  - **BE**: thêm record `AiCriterion(name, score, comment)`; `AiGradeResult` thêm `List<AiCriterion> criteria`. `GeminiAiGradingAdapter` yêu cầu prompt trả đúng 3 tiêu chí, validate schema và serialize `criteria` vào `ai[]`.
  - **FE**: thêm `IAiCriterion` + `criteria?` vào `IAiGradeDetail`. Thiết kế lại [QuestionGradeCard.tsx](file:///d:/react-exam-online/src/apps/speaking-practice/components/QuestionGradeCard.tsx): header điểm tổng/band → 1 nhận xét chung → khối "Điểm theo tiêu chí" với mỗi tiêu chí là MỘT HÀNG ngang gọn (tên | thanh `Progress` tô màu | điểm), không còn đoạn nhận xét dài từng tiêu chí nên form ngắn, không phải scroll. Card chỉ hiển thị (hành động chấm ở nút `Chấm bằng AI` dưới footer). `tsc --noEmit` sạch.
  - _Điều chỉnh sau đó_: **điểm tiêu chí đổi sang thang 0–50** đúng thang quy đổi Aptis (điểm tổng `score` gốc vẫn 0–100). BE: prompt yêu cầu `score` tiêu chí 0-50 + nhắc chấm theo thang Aptis Writing/Speaking, `parseCriteria` clamp 0-50, schema Gemini `maximum` 50. FE: `colorByFraction` tô màu theo tỉ lệ (dùng chung tổng 0–100 & tiêu chí 0–50), thanh `Progress` quy đổi %, nhãn hiển thị `{score}/50`. Bỏ `margin-top` ô chấm AI; giảm chiều cao khối ghi âm [SpeakingController.tsx](file:///d:/react-exam-online/src/apps/speaking-practice/pages/speaking-part1/components/SpeakingController.tsx) (`min-height` 320→220, `padding` 2.5rem→1.5rem) — dùng chung nên áp dụng luôn Part 1-4.
- _2026-07-31_: **Bỏ menu động, chuẩn hóa RBAC cố định theo vai trò** (cross-repo `be-exam-online` + FE): xóa toàn bộ cơ chế phân quyền menu cấu hình trong DB, chuyển hẳn sang RBAC cố định `users.role` → JWT → `@PreAuthorize`.
  - **BE**: xóa module `menu/` (13 file: controller, request/response, application service, 2 entity `SystemMenuEntity`/`RoleMenuAccessEntity` + id, 2 repository) và test liên quan; gỡ 2 `@MockitoBean` menu khỏi `ExamOnlineApplicationTests`; thêm migration `V4__drop_dynamic_menu_tables.sql` (DROP `role_menu_access` rồi `system_menus`). `compile` sạch.
  - **FE**: thêm nguồn phân quyền dùng chung [roleAccess.ts](file:///d:/react-exam-online/src/shared/auth/roleAccess.ts) (`UserRole` ADMIN/TEACHER/STUDENT, `ROLE_ACCESS`, `hasAnyRole`, `roleHomePath`); hợp nhất kiểu `UserRole` (auth + admin-users re-export). Guard route [RequireRole.tsx](file:///d:/react-exam-online/src/shared/auth/RequireRole.tsx): chờ bootstrap phiên, chưa đăng nhập → `/login`, sai vai trò → trang mặc định của vai trò (TEACHER → `/admin/questions/grammar`). Guard khu `/admin` (ADMIN+TEACHER) và 4 trang chỉ ADMIN (Tổng quan, Người dùng, Cài đặt, Thông báo) trong [routes.tsx](file:///d:/react-exam-online/src/apps/admin/routes.tsx). [AdminLayout.tsx](file:///d:/react-exam-online/src/apps/admin/components/AdminLayout.tsx) lọc menu theo vai trò + hiển thị đúng tên/vai trò người dùng đang đăng nhập. Gỡ `PermissionMatrix` giả (checkbox/lưu mock) + `initialPermissions` + `permissions/setPermissions`, thay bằng bảng chỉ-đọc [RoleInfo.tsx](file:///d:/react-exam-online/src/apps/admin/pages/admin-users/components/RoleInfo.tsx). Chuẩn hóa [axios.ts](file:///d:/react-exam-online/src/configs/axios.ts): 403 không refresh/logout, báo "Bạn không có quyền thực hiện chức năng này.". `tsc --noEmit` sạch, eslint file mới sạch.
  - **Docs**: cập nhật `DATABASE_DESIGN.md` (BE + FE): 19→17 bảng, gỡ `system_menus`/`role_menu_access` khỏi ER/mô tả/khóa ngoại, thêm mục "Phân quyền ứng dụng (RBAC cố định)".
- _2026-07-31_: **Đồng bộ thông điệp lỗi lên FE, bỏ báo lỗi chung chung** (đặc biệt cho chấm AI Writing/Speaking):
  - [axios.ts](file:///d:/react-exam-online/src/configs/axios.ts): tách hàm `resolveErrorMessage` — ưu tiên dùng đúng `message` backend trả về; khi không có phản hồi thì phân biệt **timeout** ("Máy chủ xử lý quá lâu (chấm AI có thể mất nhiều thời gian)...") với **mất kết nối** ("Không thể kết nối tới máy chủ...") thay vì gộp thành một câu chung; có status nhưng không message → "Máy chủ trả về lỗi {status}". Giữ nguyên 403 (thiếu quyền) và luồng refresh 401.
  - [usePerQuestionGrading.ts](file:///d:/react-exam-online/src/apps/speaking-practice/hooks/usePerQuestionGrading.ts): lỗi chấm AI dùng đúng thông báo backend/interceptor và cho phép nộp lại; không tạo kết quả tạm.
  - **Tiêu đề notification theo loại lỗi**: thêm `resolveErrorTitle` trong [axios.ts](file:///d:/react-exam-online/src/configs/axios.ts) — thay vì luôn "Lỗi hệ thống", đổi theo loại: 400 "Dữ liệu không hợp lệ", 401 "Đăng nhập thất bại", 403 "Không đủ quyền", 404 "Không tìm thấy", 409 "Dữ liệu bị xung đột", 413 "Tệp quá lớn", ≥500 "Lỗi máy chủ"; không phản hồi → "Quá thời gian chờ"/"Mất kết nối". **Verify trình duyệt**: submit login khi BE tắt → notification hiện đúng tiêu đề "Mất kết nối" + nội dung cụ thể, console không lỗi.
- _2026-08-01_: **Hoàn thiện Learning Streak end-to-end** (Spring Boot + React): một ngày được tính khi STUDENT submit thành công ít nhất một đề; nhiều submit cùng ngày chỉ tính một lần.
  - **BE**: ngày nghiệp vụ cấu hình bằng `LEARNING_STREAK_ZONE_ID` (mặc định `Asia/Ho_Chi_Minh`); ghi `learning_streaks` bằng PostgreSQL atomic upsert; API `/streaks/me` trả `currentStreak = 0` khi chuỗi đã hết hiệu lực nhưng giữ `longestStreak`; `/users` dùng response quản trị có streak summary và bulk-load để tránh N+1.
  - **FE học viên**: thêm `ILearningStreak`, `studentExamApi.myStreak`, `useMyStreakQuery`, cache key dùng chung và invalidate sau submit; thay số 12/5 ghi cứng tại Home cùng các landing Grammar/Reading/Listening/Speaking/Writing bằng dữ liệu API thật.
  - **FE admin**: xóa `fakeStreak`, thay streak trong danh sách/chi tiết user và bảng admin progress bằng dữ liệu `/users`; các thống kê admin progress chưa có API hiển thị `—` thay vì trộn mock với streak thật.
  - **Kiểm tra**: Java compile sạch, 8 test streak pass, React production build pass và ESLint scoped phần streak sạch. PostgreSQL/Testcontainers staging còn cần chạy khi Docker khả dụng.
- _2026-08-06_: **Dọn deprecation antd v6 + dashboard dùng dữ liệu thật + bắt đăng nhập trước khi luyện tập**:
  - **Deprecation antd v6**: Drawer `bodyStyle`→`styles.body` và `width`→`size` (10 Drawer: home/materials/admin layout + 5 landing kỹ năng + speaking/writing Index); notification `message`→`title`; Spin `tip`→`description` (7 chỗ). Khử cảnh báo *"Static function can not consume context"*: bọc [App.tsx](file:///d:/react-exam-online/src/App.tsx) trong antd `<App style={{display:contents}}>` (giữ nguyên layout + tương thích cssVar), thêm `<NotificationBridge>` đưa instance notification ra [configs/notification.ts](file:///d:/react-exam-online/src/configs/notification.ts) cho axios interceptor dùng; [useAuthAction](file:///d:/react-exam-online/src/apps/home/hook/useAuthAction.ts) chuyển sang `App.useApp()`.
  - **4 thẻ thống kê dashboard bỏ mock** ([useHomeData.ts](file:///d:/react-exam-online/src/apps/home/hook/useHomeData.ts), [StatsSection.tsx](file:///d:/react-exam-online/src/apps/home/components/StatsSection.tsx)): Tiến độ Tổng quan tính từ `/progress/me` (tổng đã làm/tổng câu), Chuỗi học tập từ `/streaks/me`, Điểm dự đoán = `averageMockScore` từ `/attempts/me` (thang /100), Mục tiêu Trình độ = "B2" (hằng số nền tảng, chưa có API mục tiêu riêng). **Khách chưa đăng nhập → cả 4 thẻ hiển thị "—"** (không còn số ảo 26%/B2/139), và các query student `enabled: isAuthenticated` nên không gọi API/không sinh 401 khi là khách.
  - **Bắt đăng nhập trước khi dùng trang** ([RootAuthGate.tsx](file:///d:/react-exam-online/src/shared/auth/RootAuthGate.tsx) đặt tại `rootRoute`): guard tập trung chặn cả điều hướng nội bộ lẫn URL trực tiếp. Trang public: `/` (dashboard), `/login`, `/register`, `/oauth`, và **`/faq` (Góc giải đáp)**; mọi trang luyện tập/thi thử/tài liệu/hồ sơ bắt buộc đăng nhập → về `/login`.
  - **Verify trình duyệt (khách)**: dashboard 4 thẻ "—"; vào thẳng `/reading` → redirect `/login`; `/faq` vào được bình thường; console sạch cảnh báo antd; không có request `/progress|/streaks|/attempts`. `tsc --noEmit` sạch.
  - _Bổ sung sau đó_: **Dọn nốt lỗi ESLint có sẵn** ở [AdminLayout.tsx](file:///d:/react-exam-online/src/apps/admin/components/AdminLayout.tsx) (`currentAcc as any` → `as LinkProps['to']`), [speaking-practice/Index.tsx](file:///d:/react-exam-online/src/apps/speaking-practice/pages/Index.tsx) + [writing-practice/Index.tsx](file:///d:/react-exam-online/src/apps/writing-practice/pages/Index.tsx) (bỏ setter không dùng `setSpeakingProgress/setWritingProgress/setMockProgress`, `catch (e)`→`catch`, `navigate to ... as any`→bỏ cast, `setActiveTab(value as any)`→`as 'parts' | 'mock-tests'`).
  - **Khử 2 lỗi 401 khi mở app lúc chưa đăng nhập**: nguyên nhân là bootstrap luôn gọi `/auth/account` → 401 rồi interceptor thử `/auth/refresh` → 401 (khách không có refresh cookie). Sửa [authQuery.ts](file:///d:/react-exam-online/src/apps/auth/services/authQuery.ts): `useAccountQuery` thêm `enabled: !!tokenManager.getAccessToken()` — chỉ khôi phục phiên khi CÓ access token đã lưu; token hết hạn vẫn được interceptor refresh qua cookie như cũ. **Verify trình duyệt (xóa localStorage → khách)**: 0 request tới backend `:6969`, console không còn lỗi 401, dashboard vẫn render. `tsc --noEmit` + eslint sạch.
- _2026-08-06_: **Mock Exam Center: thang điểm 0–200 + sửa spinner lệch trái**:
  - **Điểm mock đổi sang thang Aptis 0–200** (4 kỹ năng Nghe/Đọc/Viết/Nói × 0–50; Grammar là Core riêng, không cộng — trùng cách BE tính `overallCefr`). Thêm helper [cefrScale.mockTotalScaled](file:///d:/react-exam-online/src/shared/utils/cefrScale.ts) cộng `scaled` 4 kỹ năng từ snapshot `skillCefr`. [useMockExamLanding.ts](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/mock-exam-landing/hook/useMockExamLanding.ts): điểm từng đề + lịch sử + **điểm trung bình** tính từ `skillCefr` (0–200) thay cho `averageMockScore` (0–100); `TARGET_SCORE = 160`. [Index.tsx](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/mock-exam-landing/pages/Index.tsx) hiển thị `/200`; [ExamResultScreen.tsx](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/main-mock-exam/components/ExamResultScreen.tsx) "Điểm tổng" dùng `mockTotalScaled(skills)` `/200` (điểm AI thô mỗi câu giữ `/100` vì là thang riêng 0–100).
  - **Dữ liệu thật**: xác nhận card banner đã nối API thật — Điểm Trung Bình & lịch sử từ `/attempts/me` (snapshot `skillCefr`), "Trình độ hiện tại" = `overallCefr` band thật của lần thi gần nhất (không thuần điểm), Tiến độ mục tiêu = điểm TB / 160.
  - **Spinner loading lệch trái**: trước đây `<Spin>` nằm trong `TestListSection` (cột trái grid `1fr 350px`) nên canh giữa cột trái. Chuyển sang khối full-width `justify-content:center` đặt thẳng trong `Container` → canh giữa toàn trang.
  - **Điểm dự đoán ở dashboard Home đổi sang /200** ([useHomeData.ts](file:///d:/react-exam-online/src/apps/home/hook/useHomeData.ts)): bỏ `averageMockScore` (0–100 của BE), tính `predictedScore` = trung bình tổng `mockTotalScaled(skillCefr)` các attempt `MOCK_TEST` (0–200), nhất quán với Mock Exam Center; hiển thị `…` khi đang tải, `—` khi chưa có dữ liệu.
  - **Verify trình duyệt (khách)**: HomePage render bình thường (4 thẻ "—"), module mock-exam Index.tsx transform 200 OK, `tsc --noEmit` sạch. **Chưa verify được số /200 thực tế** ở trang có dữ liệu vì cần phiên đăng nhập — Browser pane trong môi trường này là trình duyệt riêng, không mang phiên localhost:5173 của người dùng và không được phép tự nhập thông tin đăng nhập; cần người dùng tự kiểm tra trên Chrome đã đăng nhập.
- _2026-08-06_: **Sửa gốc spinner "Đang tải" lệch trái ở loader toàn trang** (báo lại: các trang hiện chữ "Đang tải" + vòng xoay nằm sát lề trái):
  - **Nguyên nhân gốc**: [index.css](file:///d:/react-exam-online/src/index.css) đặt `body`/`#root` là `display:flex` (pattern full-height), còn [App.tsx](file:///d:/react-exam-online/src/App.tsx) bọc `<App style={{display:contents}}>`. Do `display:contents`, các component render THẲNG ở root (không qua layout) trở thành flex-item trực tiếp của `#root` (flex row). Div block thường không có `width/flex` sẽ co theo nội dung (~65px) và dồn sát lề trái → spinner trông lệch trái dù đã có `justify-content:center`. Đo thực tế trong trình duyệt: wrapper rộng 65px, `left:0`.
  - **Sửa**: thêm `width:100%; flex:1` cho 3 loader full-page render ở root: [RootAuthGate.FullScreenSpin](file:///d:/react-exam-online/src/shared/auth/RootAuthGate.tsx) ("Đang tải..." khi khôi phục phiên/F5 trang cần đăng nhập), [FullPageCenter](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/main-mock-exam/styles/shared.styles.ts) ("Đang tải đề thi thử..." + màn lỗi/hoàn thành của bài thi), [CenterWrapper](file:///d:/react-exam-online/src/apps/auth/pages/oauth-callback/styles/oauthCallback.styled.ts) (màn xử lý OAuth).
  - Các loader trong landing 5 kỹ năng/materials/faq KHÔNG dính lỗi vì nằm trong `DashboardLayout` (không phải flex-item trực tiếp của `#root`).
  - **Verify trình duyệt**: ép hiện `FullScreenSpin` (stall `/auth/*`), đo lại sau sửa: wrapper rộng **1280px**, tâm spinner **640 = giữa màn hình** (trước sửa: wrapper 65px, dồn trái). `tsc --noEmit` sạch.
- _2026-08-06_: **Đồng bộ màu Speaking part + chuẩn hoá mục tiêu Mock về B2**:
  - **Màu chủ đạo 4 trang luyện Nói về navy đồng bộ**: trước đây mỗi part có accent riêng (part1 `#0284c7` xanh, part2 `#059669` lục, part3 `#ea580c` cam, part4 `#7c3aed` tím) dùng cho viền QuestionBox, statusColor, icon mở khoá, nút chọn mẫu. Đổi hết về `#1a365d` (navy thương hiệu, trùng các trang thực hành khác như reading-mock-test). Giữ nguyên `#3b5b8c` (nút ghi âm, đã là navy), `#fbbf24`/`#10b981` (semantic đồng hồ/tiến độ). KHÔNG đụng bộ màu part-card ở các trang landing vì bộ đó dùng chung nhất quán cho mọi kỹ năng.
  - **Mục tiêu Mock Exam Center: C → B2** (đồng bộ default nền tảng đã dùng ở Home/AuthLayout/Sidebar). Thêm helper [cefrScale.cefrTargetTotal](file:///d:/react-exam-online/src/shared/utils/cefrScale.ts) tính điểm tổng tối thiểu (0–200) để đạt 1 band trên cả 4 kỹ năng = tổng ngưỡng vào band từng kỹ năng (A1=26, A2=66, B1=102, **B2=153**, C1=184). [useMockExamLanding.ts](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/mock-exam-landing/hook/useMockExamLanding.ts): `TARGET_LEVEL = DEFAULT_TARGET_CEFR ('B2')`, `TARGET_SCORE = cefrTargetTotal('B2') = 153` (thay hằng số 160 tuỳ ý); [Index.tsx](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/mock-exam-landing/pages/Index.tsx) hiển thị "Mục tiêu B2 · 153 điểm". Home [useHomeData.ts](file:///d:/react-exam-online/src/apps/home/hook/useHomeData.ts) dùng chung `DEFAULT_TARGET_CEFR` (một nguồn sự thật).
  - **Verify**: `tsc --noEmit` + eslint sạch; 3 module đã sửa transform 200 OK; `cefrTargetTotal` kiểm bằng tay ra đúng ngưỡng từng band. Phần nhìn trực quan (trang Nói + mock sau đăng nhập) cần người dùng xem trên Chrome đã đăng nhập.
  - _Bổ sung_: đồng bộ nốt màu Nói ở **trang thi thử theo bộ đề**: [speaking-mock-test/Index.tsx](file:///d:/react-exam-online/src/apps/speaking-practice/pages/speaking-mock-test/pages/Index.tsx) (PART_META + inline: 4 accent → `#1a365d`) và **section Nói trong Mock Exam đầy đủ** [SpeakingSection.tsx](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/main-mock-exam/components/SpeakingSection.tsx) (`PART_COLOR` toàn navy). `pages/mock-tests/` là dead code (không route/import) nên bỏ qua. `tsc`+eslint sạch, 2 module transform 200. **Lưu ý**: trong Mock Exam đầy đủ, `ReadingSection`/`WritingSection` vẫn còn accent riêng theo part — chưa đổi (chờ yêu cầu nếu muốn đồng bộ nốt).
- _2026-08-06_: **Đồng bộ nốt Writing/Reading (thi thử + bộ đề) theo bản "theo phần"**:
  - **Màu navy nốt phần Viết**: [WritingSection.tsx](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/main-mock-exam/components/WritingSection.tsx) thêm `PART_ACCENT = '#1a365d'` cho cả 4 part (kể cả fallback của `InstructionBox`); [writing-mock-test/Index.tsx](file:///d:/react-exam-online/src/apps/writing-practice/pages/writing-mock-test/pages/Index.tsx) `PART_META.color` 4 part + 2 `InstructionBox` inline → `#1a365d`. Giữ nguyên `AVATAR_COLORS` (phân biệt thành viên chat) và `bandColor` (semantic). `ReadingSection` không có accent theo part nên không đổi.
  - **Bỏ nhãn "Part N" trùng lặp** ở Writing bộ đề: xoá `<Tag color="blue">{activeMeta.label}</Tag>` dưới subtitle (subtitle đã ghi "Writing Part N • …"). `label` vẫn dùng cho bảng câu hỏi bên phải.
  - **Đồng bộ UI Reading Part 2/3 (kéo thả)** ở **thi thử** ([ReadingSection.tsx](file:///d:/react-exam-online/src/apps/full-mock-exam/pages/main-mock-exam/components/ReadingSection.tsx)) và **bộ đề** ([reading-mock-test/Index.tsx](file:///d:/react-exam-online/src/apps/reading-practice/pages/reading-mock-test/pages/Index.tsx)) theo bản "theo phần" (chuẩn): tiêu đề cột "Sắp xếp đoạn văn" / "← Kéo hoặc click để chọn câu" + bộ đếm `x/y Đã xếp` (styled `OrderingColumnHeader` mới), placeholder ô trống "Kéo câu vào đây cho vị trí số {idx}", thông báo "✓ Đã sắp xếp tất cả các câu!" (`CompletedMessage`), bảng đáp án đúng chuyển từ inline style sang styled (`CorrectAnswerRow`/`CorrectBadgeNumber`/`CorrectAnswerText`).
  - **Styles kéo thả khớp bản chuẩn**: viền mặc định `PlacedItemCard` `#3b82f6` → `#1a365d`, thêm shadow nhẹ cho `FixedSentenceCard`/`DraggableCard`, `padding-right` phần text, hover đỏ đậm nút ✕, `cursor: grabbing` khi kéo, grip handle in đậm — áp cho cả `reading-mock-test/styles/styled.ts` và `main-mock-exam/styles/reading.styles.ts`.
  - **Verify**: `tsc --noEmit` sạch; eslint sạch trên các file đã sửa (2 lỗi còn lại thuộc `speaking/AudioWaveform.tsx` + `SpeakingController.tsx`, có sẵn từ trước). Phần nhìn trực quan cần người dùng xem trên Chrome đã đăng nhập.
- _2026-08-08_: **Quên mật khẩu qua OTP email (cross-stack: Spring Boot + React)** — luồng 3 bước: gửi OTP → xác thực OTP → đặt lại mật khẩu.
  - **Backend (java-exam-online)**: thêm `spring-boot-starter-mail`; cấu hình `spring.mail.*` (Gmail SMTP) + `app.otp.*` trong `application.yml`. `OtpProperties` (@ConfigurationProperties `app.otp`): length=6, ttl=5m, max-attempts=5, resend-cooldown=60s, reset-token-ttl=10m, backend=memory|redis. Tầng lưu trữ trừu tượng `PasswordResetStore` (interface) + `InMemoryPasswordResetStore` (default, `@ConditionalOnProperty matchIfMissing`) + `RedisPasswordResetStore` (khi `app.otp.backend=redis`) — mirror pattern rate-limit sẵn có. `PasswordResetMailer` gửi HTML MimeMessage; nếu chưa cấu hình SMTP thì log OTP ra console (dev fallback). `PasswordResetService`: forgotPassword (không lộ email tồn tại — luôn trả message chung; chặn spam theo cooldown), verifyOtp (so sánh hash SHA-256 constant-time, giảm dần số lần thử, cấp reset-ticket 32 byte base64url khi đúng), resetPassword (đổi mật khẩu qua ticket + thu hồi toàn bộ refresh session). 3 endpoint `POST /auth/{forgot-password,verify-otp,reset-password}` (đã thêm vào permitAll của SecurityConfiguration). Compile sạch.
  - **Frontend**: module `apps/auth/pages/forgot-password` — [ForgotPasswordPage.tsx](file:///d:/react-exam-online/src/apps/auth/pages/forgot-password/pages/ForgotPasswordPage.tsx) (Smart, 4 bước: email → 6 ô OTP có auto-advance/paste/đếm ngược gửi lại → mật khẩu mới → màn thành công), [useForgotPassword.ts](file:///d:/react-exam-online/src/apps/auth/pages/forgot-password/hook/useForgotPassword.ts) (orchestrate 3 mutation + cooldown 60s), [forgotPassword.styled.ts](file:///d:/react-exam-online/src/apps/auth/pages/forgot-password/styles/forgotPassword.styled.ts) (tái dùng login.styled). Thêm types/authApi/authQuery cho 3 API. Route `/forgot-password` vào `authRoutes`; link "Quên mật khẩu?" ở LoginPage trỏ `/forgot-password`. `tsc --noEmit` sạch.
  - **Cấu hình cần thiết**: đặt biến môi trường `MAIL_HOST=smtp.gmail.com`, `MAIL_PORT=587`, `MAIL_USERNAME=<email>`, `MAIL_PASSWORD=<Gmail App Password>` cho backend. Chưa có 2 biến này → OTP in ra console log (dev). **Chưa verify trình duyệt** vì cần backend chạy + SMTP; người dùng tự kiểm thử end-to-end.
- _2026-08-09_: **Kết quả & Lịch sử thi nối API thật + bỏ trang Tiến độ học viên**:
  - **Grading = nối API thật (FE-only, không đụng BE/DB)**: backend đã có sẵn `GET /attempts` (Admin/Teacher, phân trang + lọc `studentId/status/type`) trả dữ liệu TỔNG HỢP mỗi lượt nộp (`totalScore`, `overallCefr`, `skillCefr` snapshot band từng kỹ năng). Bỏ hoàn toàn mock: xoá [admin-grading/services/mockData.ts], [pages/Detail.tsx], [hook/useDetailGrading.ts]. Thêm service [attemptApi.ts](file:///d:/react-exam-online/src/apps/admin/pages/admin-grading/services/attemptApi.ts) + [attemptQuery.ts] + [types.ts]. [useGrading.ts](file:///d:/react-exam-online/src/apps/admin/pages/admin-grading/hook/useGrading.ts) dùng `useAttemptsQuery` + `usePagination`, tab lọc theo loại đề (Tất cả / MOCK_TEST / SKILL_FULL_SET; PART_PRACTICE không ghi attempt). [Index.tsx](file:///d:/react-exam-online/src/apps/admin/pages/admin-grading/pages/Index.tsx): bảng học viên/đề/loại/trạng thái/điểm tổng/CEFR tổng/ngày nộp, **expandable row** hiện band CEFR + điểm từng kỹ năng từ `skillCefr`.
  - **Lý do bỏ trang detail cũ**: DB chỉ lưu tổng hợp — KHÔNG lưu câu trả lời/audio/nhận xét AI/4 tiêu chí (chấm AI lúc nộp trả về học viên 1 lần rồi xoá media). Muốn dựng lại detail bịa đó phải thêm bảng DB → không làm.
  - **Bỏ hẳn "Tiến độ học viên"** (trùng ~70% Dashboard, biểu đồ kỹ năng là mock): xoá dir `admin-progress/`; gỡ route (`adminProgressRoute`) khỏi [routes.tsx] + [App.tsx]; gỡ menu item + `BarChartOutlined` + breadcrumb + selectedKey ở [AdminLayout.tsx]; bỏ dòng "Tiến độ học viên" ở bảng [RoleInfo.tsx].
  - **Verify**: `tsc --noEmit` sạch; dev server build OK, `/admin/grading` redirect `/login` (Browser pane không có phiên), console sạch. **Chưa verify được bảng render thực tế** vì cần đăng nhập Admin/Teacher — người dùng tự kiểm trên Chrome đã đăng nhập.
- _2026-08-17_: **Tài liệu kiến trúc & luồng đi của code (2 file mới trong `.docs/`)**:
  - [BE_ARCHITECTURE_FLOW.md](file:///d:/react-exam-online/.docs/BE_ARCHITECTURE_FLOW.md) — backend `D:\be-exam-online\java-exam-online` (Spring Boot 4.1 / Java 21): modular monolith package-by-feature, 4 tầng `api → application → domain ← infrastructure` + Ports & Adapters (`AiGradingPort`, `SpeechToTextPort`, `StoragePort`); vòng đời request qua 6 filter (CORS → CookieCsrf → CorrelationId → BearerToken → RateLimit → authorize) tới controller/service/repository; envelope `ApiResponse` + `GlobalExceptionHandler`; JWT HS256 50m + refresh cookie httpOnly 7d; RBAC `@PreAuthorize` theo 3 nhóm; bóc tách chi tiết `POST /exams/{id}/submit` (idempotency → chấm tự động → chấm AI đồng bộ → tính CEFR → persist → cleanup media); chuỗi ASR local→groq→Gemini; upload qua magic-bytes; bản đồ toàn bộ endpoint + checklist thêm API mới.
  - [FE_ARCHITECTURE_FLOW.md](file:///d:/react-exam-online/.docs/FE_ARCHITECTURE_FLOW.md) — frontend: cấu trúc đệ quy `services/ hook/ components/ styles/ pages/` mỗi trang; đường đi `Component → hook → query → api → axios`; tầng axios (bóc envelope, cờ `_rawEnvelope`, refresh 401 gộp promise + `navigator.locks`, notification tập trung, 401 im lặng ngoài login/register); React Query key + invalidate; guard 2 lớp `RootAuthGate` / `RequireRole` + `roleAccess` (đang `BYPASS_ROLE = true`); luồng làm & nộp bài (upload audio `Content-Type: null`, submit timeout 120s); chuẩn UI nền `#f8fafc` + phân biệt card trang chủ (chỉ shadow) vs card trang kỹ năng (có border); bảng lỗi hay gặp + checklist thêm màn hình mới.
  - Tài liệu viết từ đọc mã nguồn thực tế của cả 2 repo (không suy đoán); đã đối chiếu `App.tsx`, `configs/axios.ts`, `shared/auth/*`, `admin-users/*`, `SecurityConfiguration`, `ExamSubmissionService`, `application.yml`.
- _2026-08-20_: **Sửa lỗi nộp bài treo `PROCESSING` + 409 kẹt cứng (cross-stack)** — triệu chứng: chấm AI chậm → FE timeout 120s → phiếu `exam_submit_requests` kẹt `PROCESSING` → mọi lần bấm nộp lại đều 409 trong 10 phút.
  - **Nguyên nhân gốc**: ngân sách chờ của BE (`GEMINI_REQUEST_TIMEOUT 45s × GEMINI_MAX_ATTEMPTS 3 ≈ 136s/câu`, concurrency 3 → nhiều đợt) **lớn hơn** `timeout: 120_000` của FE. FE luôn bỏ cuộc trước khi BE kịp trả lỗi, nên `release()` (xoá phiếu) không bao giờ chạy kịp phía người dùng.
  - **BE (java-exam-online)** — cân lại ngân sách trong `application.yml` + `.env` + `.env.example`: `GEMINI_REQUEST_TIMEOUT 45s → 90s`, `GEMINI_MAX_ATTEMPTS 3 → 2`, `GEMINI_RETRY_BACKOFF 500ms → 1s`, `GEMINI_MAX_CONCURRENCY 3 → 8` (cả đề chấm trong 1 đợt) → worst case ≈ **181s**. `EXAM_SUBMIT_IDEMPOTENCY_STALE_AFTER 10m → 5m`.
  - **Bất biến mới (ghi thành comment tại chỗ)**: `ngân sách chấm AI (~181s) < idempotency-stale-after (5m) ≤ FE submit timeout (300s)`. Vế trái chặn việc request retry cướp `owner_token` của lượt chấm còn đang chạy (gây chấm 2 lần, tốn quota Gemini); vế phải đảm bảo khi FE đã bỏ cuộc thì user nộp lại được ngay.
  - **FE** — [studentExamApi.ts](file:///d:/react-exam-online/src/shared/services/student-exam/studentExamApi.ts): `SUBMIT_TIMEOUT_MS = 300_000` thay cho `120_000`.
  - **FE** — [submissionIdempotency.ts](file:///d:/react-exam-online/src/shared/services/student-exam/submissionIdempotency.ts): key giờ gắn với **vân tay nội dung bài làm** (`submissionFingerprint`: sort theo `questionId` rồi FNV-1a + djb2, khớp cách BE băm request). Nộp lại **cùng nội dung** → giữ key (vẫn chống chấm trùng); **sửa đáp án rồi nộp lại** → tự cấp key mới. Diệt tận gốc lỗi 409 `"Idempotency-Key đã được dùng cho nội dung nộp bài khác"` — trước đây kẹt tới 1 ngày vì key chỉ bị xoá khi nộp thành công. `sessionStorage` chuyển sang lưu JSON `{key, fingerprint}`, dữ liệu định dạng cũ tự bị bỏ qua.
  - **Verify**: `tsc --noEmit` + eslint sạch trên 2 file FE; cấu trúc `application.yml` giữ nguyên. **Chưa chạy thử end-to-end** — cần backend + Gemini gọi được. Nếu vẫn lỗi "AI chưa thể chấm bài lúc này", nguyên nhân nằm ở kết nối/quota Gemini, xem log `AI grading failed for question`.
- _2026-08-20 (tiếp)_: **Truy nguyên & sửa dứt điểm lỗi 503 "AI chưa thể chấm bài lúc này" (backend `java-exam-online`)** — đo bằng probe chạy đúng lớp thật (`GeminiAiGradingAdapter` + `FallbackSpeechToText` + `HttpGeminiClient`), không đoán.
  - **Nguyên nhân gốc (chuỗi 4 mắt xích)**: `FallbackSpeechToText` dựng `HttpClient` không ghim version → Java mặc định **HTTP/2**, với endpoint `http://` nó gửi kèm `Upgrade: h2c`; uvicorn không hỗ trợ nên **mất phần body multipart**, FastAPI trả `422 {"loc":["body","file"],"msg":"Field required"}`. ⇒ **chuỗi STT chưa từng chạy được lần nào**. Không có transcript → `gradeSpeaking()` rơi xuống tầng cuối là **đẩy thẳng audio thô lên Gemini**: đo được 1 bản ghi (0.61MB) = **44.3s**, 3 bản ghi (1.82MB) = **91.8s** dù chạy một mình. Chạy song song thì Gemini trả **`503 UNAVAILABLE - "This model is currently experiencing high demand"`** (probe 10 câu/concurrency 3 → **0/10 thành công**). Mà `GEMINI_REQUEST_TIMEOUT` khi đó là 45s — **thấp hơn cả sàn đo được của 1 câu Speaking**, nên chấm Nói về lý thuyết không bao giờ xong. Không ai thấy được chuỗi này vì `HttpGeminiClient` **vứt body lỗi** của Gemini và adapter gộp mọi RuntimeException về một câu chung.
  - **Sửa 1 — [FallbackSpeechToText.java]**: ghim `.version(HttpClient.Version.HTTP_1_1)`; đồng thời tôn trọng cờ `enabled` của từng tầng khi dựng provider (trước đó chỉ xét `configured()` nhìn url/api-key, nên `STT_LOCAL_ENABLED=false` không tắt được tầng đã có url) và truyền `httpClient` chung cho cả tầng `groq`.
  - **Sửa 2 — [HttpGeminiClient.java]**: thêm `errorDetail()` bóc `error.message` của Gemini (cắt 300 ký tự, không chứa API key) vào thông báo lỗi; `GeminiProviderException` có thêm cờ `overloaded` (HTTP 503) tách khỏi lỗi cấu hình/quota.
  - **Sửa 3 — [GeminiAiGradingAdapter.java]**: log kèm `exception.getClass().getSimpleName()`; `userMessage()` trả riêng *"Máy chấm AI đang quá tải; vui lòng chờ ít phút rồi nộp lại"* khi 503 quá tải, thay vì gộp chung với lỗi kỹ thuật.
  - **Sửa 4 — `application.yml` + `.env` + `.env.example`**: `GEMINI_REQUEST_TIMEOUT 90s → 120s`, `GEMINI_MAX_ATTEMPTS=2`, `GEMINI_RETRY_BACKOFF 1s → 5s` (503 "high demand" mà retry sau 1s chỉ làm nghẽn thêm), `GEMINI_MAX_CONCURRENCY 8 → 4` (tầng cuối mỗi lượt tải vài MB audio, quá song song là dính 503). Ngân sách mới ≈ **245s**, vẫn giữ bất biến `245s < idempotency-stale-after (5m) ≤ FE submit timeout (300s)`. Bật mặc định chuỗi ASR: `STT_ENABLED=true`, `STT_ORDER=local,groq`, `STT_LOCAL_ENABLED=true`, `STT_LOCAL_URL=http://127.0.0.1:8000`.
  - **Verify (đo thật, không phải suy luận)**: probe `SttDirect` cho thấy `HTTP_2 → 422 (121ms)` vs `HTTP_1_1 → 200 (4689ms)` — chứng minh đúng nguyên nhân. Probe end-to-end chấm đúng thành phần đề 414 (**6 RECORD + 4 ESSAY**) với STT bật: **THÀNH CÔNG 10/10 câu trong 66.1s**; metric `exam.stt.requests{outcome=success,provider=local}=10`, `exam.gemini.requests{outcome=success}=10`, lượt gọi Gemini **lâu nhất chỉ 6.4s** (trước đây 92–130s/câu rồi 503). `./mvnw -o compile` EXIT=0.
  - **⚠️ Điều kiện vận hành**: phải chạy sidecar ASR thì mới có tốc độ trên — `cd whisper-service && .venv/Scripts/python -m uvicorn app:app --host 127.0.0.1 --port 8000`. Sidecar tắt → circuit breaker bỏ qua sau 3s rồi rơi về gửi audio thẳng cho Gemini (chậm, dễ 503) chứ không chết luồng. Sau khi sửa **bắt buộc restart backend** để nạp lại `.env` và class mới.
- _2026-08-20 (tiếp 2)_: **Sửa tiến độ "luyện theo phần" đứng im ở 1 câu (backend `java-exam-online` + migration V6)**.
  - **Triệu chứng**: % của mọi part đứng yên dù học viên làm thêm câu mới. Kiểm dữ liệu thật: **mọi dòng `student_progress` đều `answered = 1`** bất kể `total` (vd `exam 395: 1/195`, `exam 393: 1/75`, `exam 399: 1/9`).
  - **Nguyên nhân gốc**: [ExamSubmissionService.java:102-103] chỉ **đếm số câu trả lời của RIÊNG lần nộp này** (`item.answered++`), rồi DB giữ `GREATEST(answered_cũ, answered_lần_này)` ([ProgressRepository]). Trang luyện theo phần **nộp từng câu một** (toast "Đã ghi nhận câu trả lời! Bạn có thể luyện câu tiếp theo") nên lần nào cũng gửi đúng 1 câu → `GREATEST(1, 1) = 1` vĩnh viễn. DB **không biết** câu vừa nộp là câu mới hay câu cũ vì nó chỉ nhận được con số, không nhận được `questionId`.
  - **Sửa — lưu TẬP câu đã làm thay vì số đếm**:
    - [V6__track_answered_questions_progress.sql] — `ALTER TABLE student_progress ADD COLUMN answered_question_ids INTEGER[] NOT NULL DEFAULT '{}'`.
    - [ProgressRepository] `upsertMaximumAnswered` → `upsertAnsweredQuestions`: `ON CONFLICT DO UPDATE` **hợp (union)** mảng cũ với mảng mới bằng `array_agg(DISTINCT ...)` trên `unnest(cũ || EXCLUDED)`, `answered = LEAST(total, GREATEST(answered_cũ, cardinality(tập hợp)))`. Giữ `GREATEST(progress.answered, ...)` để các dòng có trước V6 (tập rỗng nhưng answered > 0) không tụt về 0.
    - [SubmissionPersistenceService] `ProgressItem(skillId, partNumber, List<Integer> answeredQuestionIds, total)` + `answeredQuestionIdsLiteral()` sinh literal `{12,15,20}` (câu lệnh đã `CAST(... AS integer[])` nên driver không cần biết kiểu).
    - [ExamSubmissionService] `MutableProgress.answered` (int) → `answeredQuestionIds` (`LinkedHashSet<Integer>`), gom `question.getId()` khi câu trả lời không rỗng.
  - **Verify**: chạy đúng câu lệnh upsert mới trên **bảng TEMP** của DB thật (không đụng schema) qua 6 ca — dòng cũ tập rỗng `answered=1 → {101}`; câu MỚI `→ answered=2`; nộp TRÙNG `→ giữ 2`; nộp 2 câu cùng lúc `→ 4`; nộp RỖNG `→ giữ 4`; `total` giảm còn 3 `→ answered bị chặn còn 3`. `./mvnw -o compile` và `test-compile` đều EXIT=0.
  - **Đã áp dụng migration (2026-08-20)**: chạy Flyway một lần bằng công cụ dòng lệnh tạm (`Flyway.configure().dataSource(...).locations("filesystem:.../db/migration")`) nên **không phải bật `FLYWAY_ENABLED` trong `.env`** — giữ nguyên `false` cho lúc chạy app. Kết quả: `Migrating schema "public" to version "6"` → `SUCCESS`, `flyway info` hiện V4/V5/V6 đều SUCCESS, V1-V3 `BELOW_BASELINE`/`BASELINE` như cũ. Cột `answered_question_ids ARRAY NOT NULL DEFAULT '{}'::integer[]` đã có; dữ liệu cũ nguyên vẹn (`answered`/`total` không đổi, tập rỗng).
  - **Verify trên bảng THẬT**: chạy đúng câu upsert của app trong transaction rồi `ROLLBACK` — nộp câu `900001` → `ids={900001}`; nộp thêm `900002` → `ids={900001,900002}` (cộng dồn); nộp **trùng** `900002` → giữ nguyên 2 phần tử; `answered` bị chặn đúng bởi `total`; sau `ROLLBACK` dòng trở lại `ids={}` — dữ liệu thật không bị đụng. `./mvnw -o compile` EXIT=0.
  - **⚠️ Còn lại**: **restart backend** để nạp class mới (`upsertAnsweredQuestions`). `ddl-auto: validate` không phàn nàn về cột thừa vì `StudentProgressEntity` cố ý không map cột này.
  - **Không đổi FE**: FE chỉ đọc `GET /progress/exams/me`, công thức `% = round(answered/total*100)` giữ nguyên.
- _2026-08-20 (tiếp 3)_: **Đồng bộ trải nghiệm "luyện theo phần" cho cả 5 kỹ năng theo mô hình của Reading — làm xong câu nào chấm luôn câu đó; bỏ hẳn đồng hồ đếm ngược ở luyện theo phần**.
  - **Vấn đề (đã kiểm chứng, người dùng nói đúng)**: chỉ Reading nộp & chấm **từng bộ/từng câu** rồi hiện điểm ngay. Listening/Grammar/Writing bắt làm **hết cả part** mới nộp một lần → mới có điểm, sai lệch hoàn toàn với Reading. Speaking vốn đã chấm từng câu bằng AI nên chỉ cần bỏ đồng hồ.
  - **Chuẩn thống nhất (bê nguyên từ Reading)**: nộp riêng `questionId` đang làm → `POST /exams/{id}/submit` với đúng 1 phần tử `answers` → `summarizeAutoGrade(result, { skillId, partNumber })` (kỹ năng tự chấm) hoặc `usePerQuestionGrading` (kỹ năng AI chấm) → hiện `earned/total` (hoặc điểm AI + band) ngay tại chỗ → `isSubmitted` khoá ô nhập → bảng câu hỏi đánh dấu **"Đã chấm"** → nút đổi thành **"Làm lại"** để reset đúng câu đó.
  - **Listening P1–P4**: viết lại `usePart{N}Action.ts` chấm theo từng bộ/nhóm (`skillId: 2`), trang thêm Tag kết quả, `disabled={isSubmitted}` cho Select/OptionCard, nhãn bảng `Đã chấm`/`Đang làm`, footer đổi **Nộp bài này ⇄ Làm lại**.
  - **Grammar P1–P2**: `usePart{N}Action.ts` thành hook không tham số, tự gọi `usePartPracticeExam` (`skillId: 1`), chấm từng câu (P1) / từng task word-bank (P2); bỏ localStorage + auto-submit hết giờ. Thêm trạng thái thứ 3 `graded` cho `NavGridButton`/`LegendColorDot` và `QuestionNav`; `GrammarSection` tô xanh/đỏ đúng lựa chọn đã chọn dựa trên `earned` (BE ẩn đáp án đúng nên không so theo index).
  - **Writing P1–P4**: chuyển từ `useSubmitExamMutation` gộp cả part sang `usePerQuestionGrading` — mỗi đề nộp riêng, AI trả điểm/nhận xét ngay; thêm Tag `AI chấm: x/100 • Band y` ở header + `<QuestionGradeCard>` ngay dưới bài viết; `disabled={isSubmitted}` cho ô nhập; footer **Nộp & chấm câu này ⇄ Làm lại** (P4 giữ `SubmitButtonPurple`). Xoá `writing-part1/hook/useWritingTimer.ts` (không còn ai import).
  - **Dùng lại code thay vì nhân bản**: `usePerQuestionGrading` chuyển từ `apps/speaking-practice/hooks/` → **`src/shared/hooks/`** và bổ sung `resetGrade(key)` (bỏ kết quả 1 câu để làm lại); `QuestionGradeCard` + `AiGradeButton` chuyển sang **`src/shared/components/AiGrade/`** (có barrel `index.ts`). 4 hook + 5 trang Speaking đã đổi import theo.
  - **Bỏ đồng hồ ở luyện theo phần (quyết định của người dùng)**: gỡ countdown + `formatTime` + `S.TimerWrapper` ở **Reading P1–P4** (598s/1077s/900s), **Speaking P1–P4** (12 phút), Listening và Grammar; đồng hồ **chuẩn bị/ghi âm trong `SpeakingController` giữ nguyên** vì nó là cơ chế thi thật, không phải đồng hồ trang. Đồng hồ của các đề **thi thử (mock test) không bị đụng tới**.
  - **Verify**: `tsc --noEmit` sạch; eslint trên toàn bộ thư mục đã sửa **không phát sinh lỗi mới** (chỉ còn 3 file lỗi có sẵn từ trước: `grammar-mock-test/hook/useMockTest.ts`, `speaking-part1/components/AudioWaveform.tsx`, `SpeakingController.tsx`). Dev server (cổng 5173 của người dùng) transform **15/15 trang part** trả `200 OK`, console không lỗi. **Chưa bấm thử được trên trình duyệt** vì các trang này yêu cầu đăng nhập — người dùng tự kiểm trên phiên đã đăng nhập.
- _2026-08-22_: **Hiện đáp án đúng + tô xanh/đỏ sau khi chấm ở "luyện theo phần" (Reading, Listening, Grammar) và sửa điểm hiển thị bị cộng dồn cả đề**.
  - **Vấn đề 1 (người dùng báo)**: chấm xong Reading P1 chỉ hiện `(Đáp án đúng: )` rỗng, không tô màu ô nào. **Nguyên nhân gốc**: [AnswerSanitizer.java] xoá **mọi** key `correct*`/`is_correct`/`sample_answer` khỏi `GET /exams/{id}/take` (đúng — không được lộ đáp án trước khi làm), nên `mapPart*` của FE dựng `correctAnswers`/`correctOrder`/`correctIndex` từ config đã bị cắt ⇒ luôn rỗng/-1.
  - **Sửa BE — trả đáp án kèm KẾT QUẢ CHẤM (chỉ sau khi nộp)**:
    - [GradeResult.java] thêm field `Object correctResponse`.
    - [DeterministicQuestionGrader.java] thêm `correctResponse(type, extraConfig)` sinh đáp án đúng **đúng hình dạng mà FE gửi lên trong `response`** của từng dạng: MC gap-fill → `number[]`, MC đơn → `number`, SPEAKER_AGREEMENT → `('MAN'|'WOMAN'|'BOTH')[]`, ORDERING → `number[]` (index trong `options_pool`), WORD_BANK → `{slot_id: từ}`, HEADING_MATCH → `{paragraph_label: tiêu đề}`, SPEAKER_MATCH → `{speaker_index: đáp án}` (Listening) hoặc `string[]` (Reading); ESSAY/RECORD → `null`. **Logic chấm không đổi.**
    - Không phá bảo mật: trường này chỉ có trong response của `POST /exams/{id}/submit`; `GET /take` vẫn bị `AnswerSanitizer` cắt sạch như cũ.
  - **Sửa FE**: [types.ts] `IAutoGradeDetail.correctResponse`; [autoGradeSummary.ts] thêm `pickCorrectResponse()` + `correctIndexesToText()`. Các hook đổi từ đọc `data.correctAnswers` (luôn rỗng) sang **state `graded*` nạp từ response chấm**, và reset khi bấm "Làm lại"/đổi bộ:
    - **Reading P1** gap-fill → text đáp án đúng; **P2** ORDERING → cột "Đáp án đúng" dựng từ thứ tự BE trả (bỏ câu cố định, đổi index → id `sN`); **P3** SPEAKER_MATCH; **P4** HEADING_MATCH (đổi tiêu đề đúng về `value` `hN` của FE).
    - **Listening P1** tô xanh đúng/đỏ sai theo `correctIndex` thật; **P2** thêm `$wrong` cho Select + `(Đáp án đúng: …)`; **P3** như P2 (nhãn Man/Woman/Both); **P4** thêm `$status success|error` cho `OptionCard`.
    - **Grammar P1** `GrammarSection` nhận `correctIndex` → tô xanh **đáp án đúng** chứ không chỉ tô ô đã chọn; **P2** `VocabularySection` nhận `correctAnswers` → `(Đáp án đúng: …)` cho ô sai.
    - Mọi chỗ đều **fallback an toàn**: BE chưa cập nhật (không có `correctResponse`) thì chỉ ẩn phần review, không vỡ giao diện.
  - **Vấn đề 2 (người dùng báo tiếp)**: nộp 1 câu nhưng toast báo `2/45 câu đúng` trong khi header ghi `Kết quả: 2/5`. **Nguyên nhân**: BE luôn chấm **cả đề**, `details` gồm cả câu chưa làm (earned 0 nhưng vẫn cộng `total`); FE lại gộp bằng `summarizeAutoGrade(result, { skillId, partNumber })`. **Sửa**: `summarizeAutoGrade` nhận thêm `questionId`; **10 hook luyện theo phần** (Reading 1–4, Listening 1–4, Grammar 1–2) đổi sang lọc theo `questionId` ⇒ chỉ báo điểm **của đúng câu vừa nộp**. Toast cũng thống nhất một mẫu `Đã chấm xong câu N: x/y câu đúng.` (trước đó mỗi part một kiểu chữ).
  - **Verify**: `tsc --noEmit` sạch; `eslint` trên grammar/listening/reading + `shared/services/student-exam` không lỗi; BE `mvnw -o test -Dtest=DeterministicQuestionGraderTest` EXIT=0 (2 test mới: đúng hình dạng từng dạng câu, và `null` với ESSAY/RECORD).
  - **⚠️ Bắt buộc restart backend** để nạp `correctResponse` — chưa restart thì FE vẫn chạy nhưng không có đáp án để hiện.
  - **Chưa làm (ngoài phạm vi đã chốt)**: `reading-mock-test` vẫn dính đúng lỗi đáp án rỗng này ở dòng hiển thị `(Đáp án đúng: …)`.
- _2026-08-22 (tiếp)_: **Sửa ô "Tiến độ Tổng quan" ngoài trang chủ — tính sai và phải CHỈ tính luyện theo phần**.
  - **Vấn đề (người dùng báo)**: số % ở trang chủ không khớp với % trên thẻ từng phần của trang kỹ năng và bị thổi lên. **Nguyên nhân**: `useHomeData` cộng dồn `answered/total` của `GET /progress/me`, mà:
    1. bảng `student_progress` **chỉ có dòng cho phần đã từng nộp** ⇒ các phần chưa đụng tới biến mất khỏi mẫu số (làm 1 bộ Reading P1 đã ra ~11% thay vì ~0.6%);
    2. `aggregateBySkillAndPart` GROUP BY `(skill, part)` rồi **SUM qua MỌI đề** của phần đó, trong khi trang luyện và thẻ phần chỉ dùng **một** đề ⇒ hai bên không bao giờ khớp;
    3. cộng dồn theo số câu khiến phần nhiều câu nuốt trọn phần ít câu.
  - **Cách tính mới** (`useOverallPartPracticeProgress`): duyệt **đúng 18 phần luyện theo phần** (Grammar 2, Listening 4, Reading 4, Writing 4, Speaking 4), mỗi phần lấy **đúng đề mà trang luyện của phần đó sẽ mở** rồi tra % trong `GET /progress/exams/me`; phần chưa làm = 0%; kết quả = **trung bình các %**. Nhờ vậy con số ở trang chủ = trung bình các con số học viên nhìn thấy trên thẻ phần. Mock test không lọt vào vì chỉ liệt kê đề PART_PRACTICE.
  - **Gom về một nguồn sự thật** — file mới `shared/services/student-exam/partPracticeParts.ts`: `SKILL_ID`, 5 bảng `*_PART_MAP` (trước nằm rải rác, copy 5 bản trong 5 landing hook), `ALL_PART_PRACTICE_PARTS` và **`pickPartPracticeExam()`**. Trước đây `usePartPracticeExam` sort theo `createdAt` còn `usePartPracticeProgress` sort theo `isActive` rồi `createdAt` (comment ghi sai là "cùng quy tắc") ⇒ phần có nhiều đề thì trang luyện nộp vào đề này mà % lại hiện của đề khác. Nay cả 3 chỗ dùng chung một hàm chọn.
  - **Đổi kèm**: `useMyExamProgressQuery(enabled)` có tham số `enabled` để khách vãng lai không gọi API; 5 landing hook import map dùng chung + `SKILL_ID` thay số ma thuật; barrel `student-exam/index.ts` export 2 file mới.
  - **Verify**: `tsc --noEmit` sạch; eslint trên `shared/services/student-exam`, `apps/home` và 5 landing không lỗi. Trên trình duyệt (chưa đăng nhập): trang chủ render 4 ô đều `—`, **không phát sinh request nào** ⇒ nhánh `enabled=false` đúng, console không lỗi. **Số % khi đã đăng nhập cần người dùng tự đối chiếu** (đăng nhập là việc của người dùng).
  - **Dead code còn lại**: `useMyProgressQuery` + `normalizeProgress` trong `studentExamQuery.ts` nay không còn ai dùng (giữ lại vì vẫn là binding hợp lệ của endpoint `/progress/me`).
